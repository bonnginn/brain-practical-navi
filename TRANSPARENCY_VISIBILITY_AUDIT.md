# 脳表透過・教材レイヤー視認性監査

更新日: 2026-08-22

## 対象

脳表観察のゴースト表示における、大脳皮質・小脳・脳幹の表面、深部の模式レイヤー、脳底の神経・血管レイヤーを対象とします。対象は描画方針と可読性であり、解剖形状、メッシュ頂点・面、分節ラベル、連続性は変更しません。

## 修正前に確認した問題

- ゴースト表示の大脳皮質は不透明度 `0.13`、小脳・脳幹は `0.78` で、同じ透過操作の中で表面の見え方が不統一でした。
- 神経・血管・深部の補助レイヤーは不透明度 `1.0` で描かれていました。
- 補助レイヤーの直前に深度バッファを消去する分岐があり、内部にある部品も常に画面最前面へ投影され得ました。

## β候補で採用した表示方針

1. ゴースト表示では、大脳・小脳・脳幹の全表面を同じ不透明度 `0.18` で描きます。これは教材上の透過率であり、組織の物性や解剖学的境界を表す値ではありません。
2. 模式・神経血管レイヤーの通常表示は `0.78`、選択中の部位は `0.98` とします。選択中の神経・血管は白色ハイライトも併用し、細い走行を追跡しやすくします。
3. 透過時も深度バッファを消去せず、深部・神経血管・選択レイヤーを深度テスト付きで描いた後に透過表面を合成します。内部の部品が常に最前面へ出ることを避け、表面越しの位置関係を残します。
4. 表面の溝・裂ガイドは透過表面の合成後に深度テスト付きで描きます。ガイドは模式補助であり、分節境界ではありません。
5. 通常表示でも同じ不透明度・選択強調の定数を使います。`app/AtlasVolumeCanvas.tsx` の定数・関数を表示上の回帰検査の基準にします。

この方針は、実標本の奥行き、神経・血管の実在する太さ、または専門家確認済みの境界を再現するものではありません。模式レイヤーの由来・確度は [STRUCTURE_PROVENANCE.md](STRUCTURE_PROVENANCE.md) と画面上の注意書きを優先します。

## 実装範囲

- `SURFACE_GHOST_OPACITY`、`TEACHING_OVERLAY_OPACITY`、`TEACHING_OVERLAY_SELECTED_OPACITY` を定数化しました。
- ゴースト表示の描画順を、深度テスト付きの補助レイヤー → 透過表面 → 表面ガイドへ整理しました。
- 補助レイヤー前の深度クリアを廃止し、選択ハイライトの頂点だけ選択時不透明度へ補間します。
- 形状・メッシュ・分節は変更しない方針とし、形状生成、メッシュファイル、分節データ、座標、解剖学的ラベルは変更していません。

## 検証状況

- 静的回帰テスト: `tests/rendered-html.test.mjs` の対象を含む77/77、全テスト118/118、型検査、Vite本番ビルド、`git diff --check` に合格しました。
- 実ブラウザ確認: 最終ビルドの脳底動脈・脳神経・自由観察で、透過／通常切替、前後・上下方向の回転、個別選択時の白色強調を実操作しました。自由観察では内頸動脈とXII舌下神経を同時選択し、Papez回路の脳弓が固有色を保つこと、通常表示へ戻しても内部選択が見えることを確認しました。Canvasは各1、残留ローダー、画面内エラー、console error/warning、横はみ出しは0件でした。
- 全経路回帰: Windows Chrome 151、1366×768、1024×768、390×768、26経路×direct/reload＝156/156件に合格しました。missing/duplicate/fail=0、console／要求／画面内エラー、残留ローダー、横はみ出し、WebGL fallbackも0件でした。保存結果は `work/browser-audit/beta-route-audit-transparency-final-2026-08-22.json`（配布対象外）です。
- 物理端末、別GPU、別ブラウザ、公開URL: 未確認。

## 未監修・未保証

見え方の改善は表示上の変更であり、模式神経血管メッシュ、脳表領域、深部位置目安の解剖学的正確性を昇格させません。専門家レビュー、実標本との照合、連続断による境界確認が完了するまでは「検証済み」と表現しません。
