# 専門家レビュー経路監査

更新日: 2026-08-15

## 目的

専門家にしか判断できない解剖学的課題をCodexが推測で完了扱いせず、どのコミットの、どの画面を、誰が、どの根拠で判定したか再現できる状態へする。記録機能は専門家判断を代替せず、未確認表示を自動で「検証済み」へ昇格しない。

## 実装した契約

- `app/expert-review-targets.json`: A1〜D5の19件を、固定経路、方向、構造、合否基準、参照監査文書へ対応付ける正規台帳。
- `/?review=<ID>&commit=<SHA>#workspace/...`: 解剖画面を保ったまま、対象条件と入力票を表示する。
- `brain-practical-expert-review` version 1: 対象スナップショット、Git SHA、記名・所属・専門領域、判定、理由、根拠URL、スクリーンショット名、日時、URL、User-Agent、viewportを記録する。
- 氏名・所属等は初期値を持たず、localStorageや外部サービスへ保存・送信しない。利用者が押したときだけJSONをダウンロードする。
- `npm run audit:expert-review`: 19件の一意性、必須フィールド、アプリ経路、Markdownチェック票、参照監査文書、画面実装、プライバシー表示を照合する。
- `npm run validate:expert-review -- <record.json>`: 正規台帳との一致、Git SHA、記名、許可された判定語、理由、日時、http(s)根拠、ファイル名、固定URL、viewportを検証する。

## Windows実ブラウザ回帰

本番Viteプレビュー `http://127.0.0.1:4178/`、Windows Chromium、970 × 545 CSS pxで実施。

- A1を `/?review=A1&commit=60ca0f6#workspace/surface/lateral` から開き、左外側面、固定方向、7確認構造、合否基準、監査資料が同時に読めることを確認。
- 必須項目が空のとき「検証用JSONを書き出す」は無効。
- 架空のブラウザ試験名、所属、専門領域、`判定保留`、理由、http(s)根拠、アプリ画面だけのファイル名を入力すると有効化。
- 書き出し後に「A1 のレビュー記録を書き出しました。送信はしていません。」を表示し、URLと対象画面は変わらない。
- 次のA2へ移ると `review=A2&commit=60ca0f6#workspace/surface/superior` となり、対象コミットを維持して上面へ切り替わる。
- 票を折りたたむと固定パネルがDOMから外れ、再開ボタンと3Dの「向きを戻す」が利用可能。再開で同じ対象票へ戻る。
- 文書横幅970 px、横はみ出し0、Canvas 1。票は370 px、解剖モデル、方向表示、拡大縮小操作を同時に視認できた。

760 px以下では票を画面下部へ移し、スマートフォンの下部ドック利用時は安全域を含むドック上へ配置するCSSと自動回帰を追加した。今回の接続ブラウザはviewportを970 pxから変更できなかったため、この新規票そのものの760 px未満実ブラウザ操作は未確認として残す。既存の391 pxスマートフォンUI回帰を、この票の回帰証拠へ読み替えない。

## 判定

ローカル実装とPC実ブラウザ経路は合格。専門家の実判定は0件で、テストfixtureと架空のブラウザ入力は解剖学的レビューとして数えない。βのGo / No-Go条件9は引き続き「専門家待ち」。
