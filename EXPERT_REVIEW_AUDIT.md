# 専門家レビュー経路監査

更新日: 2026-08-15

## 目的

専門家にしか判断できない解剖学的課題をCodexが推測で完了扱いせず、どのコミットの、どの画面を、誰が、どの根拠で判定したか再現できる状態へする。記録機能は専門家判断を代替せず、未確認表示を自動で「検証済み」へ昇格しない。

## 実装した契約

- `app/expert-review-targets.json`: A1〜D5の19件を、固定経路、方向、構造、合否基準、参照監査文書へ対応付ける正規台帳。
- `/?review=<ID>&commit=<SHA>#workspace/...`: 解剖画面を保ったまま、対象条件と入力票を表示する。
- `brain-practical-expert-review` version 2: 正規台帳の完全な対象スナップショット、40桁Git SHA、記名・所属・専門領域、判定、理由、公開HTTPS根拠URL、アプリ画面のスクリーンショット名、日時、公開候補URL、User-Agent、viewportを記録する。
- 氏名・所属等は初期値を持たず、localStorageや外部サービスへ保存・送信しない。利用者が押したときだけJSONをダウンロードする。
- `npm run audit:expert-review`: 19件の一意性、必須フィールド、アプリ経路、Markdownチェック票、参照監査文書、画面実装、プライバシー表示を照合する。
- `npm run validate:expert-review -- <record.json>`: 単票の完全な台帳一致、40桁SHAとURLクエリの一致、記名、判定語、20文字以上の理由、公開HTTPS根拠、画面画像名、公開候補URL、viewportを検証する。localhost、短縮SHA、空の根拠、コミット不一致は正式証拠にしない。
- `npm run validate:expert-review-bundle -- <records-directory>`: A1〜D5の19対象が同じ40桁SHAで一意に揃い、少なくとも1名が神経解剖学の専門性を申告し、`要修正`・`判定保留`が残っていないことを検査する。成功しても採否台帳と画面への反映は管理上の別手順とする。

## Windows実ブラウザ回帰

ローカルViteプレビュー `http://127.0.0.1:4174/`、Windows ChromiumでUI回帰を実施。localhostでは「正式記録は公開候補HTTPSの固定URLでのみ」を表示し、JSON書き出しを無効にする。これは意図した証拠境界であり、専門家判定の回帰ではない。

- A1を `/?review=A1&commit=edfd50e214fd3684a7446274bb166eb8a4e44423#workspace/surface/lateral` から開き、左外側面、固定方向、7確認構造、合否基準、監査資料、40桁SHAが同時に読めることを確認。
- 対象コミット欄は読み取り専用で、URLのコミットと画面内の記録対象を乖離させない。
- 必須項目が空のとき「検証用JSONを書き出す」は無効。架空の名前、所属、専門領域、`判定保留`、20文字以上の理由、公開HTTPS根拠、PNG画面名を入力すると形式警告は0になるが、localhostのため書き出しは無効のまま。
- 未書き出し入力のまま `A2 →` を押すと「未書き出しの入力があります」を表示し、A1のURLと入力を保持する。
- Windows Chromium、CSS viewport 907 × 691 px、Canvas 1、横はみ出し0 px。レビュー票と脳表観察画面を同時に表示できた。

760 px未満の固定票・「観察へ／レビュー票へ戻る」・画面内破棄確認はversion 1で実ブラウザ回帰済みで、version 2は同じパネル構造に証拠案内と必須ラベルを追加した。実スマートフォンの最終表示・入力はGate 3と同じく未検証である。

## 判定

ローカル実装、PC、760 px未満の実ブラウザ経路は合格。専門家の実判定は0件で、テストfixtureと架空のブラウザ入力は解剖学的レビューとして数えない。βのGo / No-Go条件9は引き続き「専門家待ち」。
