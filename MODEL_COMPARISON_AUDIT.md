# M2 3Dモデル比較試作監査

更新日: 2026-08-15  
比較URL: `/?m2=compare#workspace/blocks/hindbrain`

## 目的と選定

β本体の3D基盤を全面的に作り直す前に、同じ後脳座標・同じ初期方向・同じ回転操作で二方式を小さく比較します。

- A 標本再構成中心: 同一格子の橋・延髄、小脳、第四脳室と、同一格子ラベルの教育用中脳区分。
- B 知識ベース模式中心: プロジェクトで造形した上・中・下小脳脚、菱形窩の位置指標、錐体、オリーブだけ。

ロードマップの優先例は内側側頭葉または脳室全景ですが、両者は根拠不足の模式形状をすでに除外したか、比較に使える知識ベース部品がありません。新たな未検証形状を比較のためだけに作らず、同一座標上に両方式が既存する後脳を方法検証用の `PROTOTYPE 01` に選びました。この試作だけでM2全体または形状監修を完了とはしません。

## UIと比較条件

- クエリ付きの監査経路だけで表示し、通常のβ画面や教材ナビには露出しない。
- 2 Canvasは回転状態を共有し、一方を矢印キーまたはドラッグで動かすと両方が同じ方向へ更新される。
- Aは `DATA-ANCHORED`、Bは `PROJECT-AUTHORED` と常時表示する。
- Bには標本組織と第四脳室を混ぜず、「実標本由来」「正解セグメンテーション」と表記しない。
- 760 px以下では横に圧縮せず、A、B、評価票を縦積みにする。主ナビと画面別設定シートは通常のスマホ専用UIを維持する。

## 実ブラウザ結果

Windows Chromiumの本番Viteプレビューで確認しました。

| 条件 | 結果 |
| --- | --- |
| PC 1364 × 727 px相当 | 2 Canvas、2比較カード、読込エラー0、文書横はみ出し0 |
| 狭幅 391 × 639 px相当 | 2 Canvasを縦積み、読込エラー0、文書横はみ出し0、設定シート導線を維持 |
| 同期操作 | Aへ `ArrowRight` を入力後、A・Bが同じ回転状態で再描画 |
| アセット | `npm run audit:models` で13部品、由来区分、共通座標、4 MiB予算を監査 |

## 評価

| 評価軸 | A 標本再構成中心 | B 知識ベース模式中心 |
| --- | --- | --- |
| 同定しやすさ | 支持表面から対象位置を推定できるが、小構造は埋もれやすい | 色と単純形状で個々を拾いやすいが、名称を外すと位置の手掛かりが乏しい |
| 位置関係 | 支持表面、尺度、個体形、第四脳室の空間関係を保つ | 相互配置は見やすいが、支持面を失い浮遊部品に見える |
| 表面品質 | 標本由来の粗さがある。教育用平滑モデルほど整っていない | 表面は単純で滑らかだが、滑らかさは正確性の根拠にならない |
| 回転時 | 前後・上下を支持表面から復元しやすい | 反対側へ回すと方向を見失いやすい |
| 着色・脱着 | 既存レイヤー機構と同じで、由来を保って着脱できる | 部品単位の着脱は容易 |
| 負荷 | 高密度小脳を含むため大きい | 単独なら軽いが、比較画面ではAとキャッシュを共有して2 Canvasを描画 |
| 制作・修正 | 元格子へ戻って再生成できる | 形状の判断と変更履歴を人手で維持する必要がある |

## 暫定判断

β本体はAの標本再構成を基盤にし、Bは由来を明示した着脱可能な教育補助としてのみ使います。B単独への置換は、支持表面、尺度、個体差、修正の再現性を失うため採用しません。

これは専門家による形状承認ではありません。次は、対象名を伏せた同定試験、スマートフォン実機のGPU・メモリ、専門家による小脳脚・菱形窩・錐体・オリーブの角度別判定を追加します。

## 参考にした実装

- [Google Neuroglancer](https://github.com/google/neuroglancer): レイヤー名から可視性と表示属性を操作する設計。比較でも由来別レイヤーを混ぜずに扱う判断の参考。
- [Papaya](https://github.com/rii-mango/Papaya): Web上の直交断面、オーバーレイ、アトラス、表面データを同一ビューアで扱う設計。
- [NiiVue](https://github.com/niivue/niivue): ブラウザで神経画像を扱うWebGLビューア。比較経路もインストール不要のWeb実装を維持した。

これらのUIを複製したものではなく、レイヤー可視性、同一条件比較、ブラウザ内処理という共通原則だけを参照しています。
