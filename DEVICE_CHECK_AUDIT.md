# 実スマートフォン診断・回帰手順

更新日: 2026-08-15  
対象: β候補 Gate 3（実機操作）・Gate 4（実機性能）

## 目的と判定範囲

`#workspace/device-check` は、端末差を同じJSON形式で記録するための補助画面です。診断値と作業中の下書きは同一オリジンの端末内だけに保存し、画面移動・PWA再起動後も再開できます。自動送信せず、JSONの書き出しを選んだ場合だけローカルファイルを作ります。画面の「下書きを初期化」で端末内記録を初期状態へ戻せます。

この診断だけでは実機ゲート合格になりません。実際のスマートフォンで主要ルートを一周し、タッチ操作、OS文字サイズ、GPU、メモリ、回線、PWA単独起動、オフライン復帰を確認する必要があります。

## 採取する情報

- 記録時刻、任意の端末メモ、現在のパスとハッシュ
- User-Agent、言語、画面・Visual Viewport・DPR・画面方向
- coarse/fine pointer、hover、最大タッチ点数、PWA単独表示
- Service Worker制御、Cache Storage、オンライン状態、取得可能な回線情報
- Storage Estimateの使用量・割当量・永続保存状態
- WebGL / WebGL 2、renderer、最大テクスチャ寸法
- `safe-area-inset-*` の実測値
- 約1秒の静止画面におけるフレーム間隔の中央値・p95・最大値
- `pointerType === "touch"` の実入力記録。マウスとペンは確認済みにしない
- 保存後オンライン、機内モード起動、再接続後の3時点における接続、PWA単独起動、Service Worker制御、教材カタログ版
- 各時点の脳表・断面・局所標本セットについて、期待ファイル数、健全な実キャッシュ数、完了マーカー、セット版
- 公開HTTPSでの初回ホーム読込について、Navigation / Resource Timing、転送量、表示領域、Canvas、横はみ出し
- オンライン6経路と機内モード4経路について、区間ごとの取得量、Canvas数、横はみ出し、利用可能なChromium系ブラウザでは250 ms間隔で観測した最大JS heap

回線情報、JS heap、GPU rendererなど、ブラウザが公開しない値は取得不可のまま記録します。約1秒のフレーム採取は操作中の性能試験の代替ではありません。250 ms間隔のJS heapは観測最大値であり、OS全体・GPU・ネイティブメモリやサンプル間の瞬間ピークを含みません。

## 実機での手順

1. 検証用端末で対象オリジンのサイトデータとブラウザキャッシュを消去し、公開候補HTTPSの `#workspace/home` を初回表示する。普段使いの保存データを消さない。
2. ほかの教材へ移る前に実機診断を開き、「性能記録を開始」を押す。直前のホーム初回読込が「初回ホーム」として記録されたこと、計測元が公開HTTPSであることを確認する。
3. PWAをホーム画面へ追加して単独起動し、脳表・断面・局所標本の3教材セットをWi-Fi上で保存する。
4. 実機診断で端末メモを任意入力し、「ここを指でタッチ」を指で押す。「タッチ確認済み」を確認して「端末診断を開始」も実行する。
5. 「保存後・オンライン」を記録し、単独起動、Service Worker制御、教材3/3保存済みを確認する。この操作で教材保存中の取得履歴を次の経路区間から切り離す。
6. ホーム、脳表、断面、局所標本、クイズ、編集ツールをこの順に実際に操作し、毎回診断画面へ戻って対応項目を確認済みにする。下書きと性能セッションは画面移動・PWA再起動後も復元される。
7. 機内モードにしてPWAを完全に閉じて再起動し、脳表、断面、局所標本、クイズをこの順に完全再読込・操作し、毎回対応項目を確認済みにする。
8. 通信を戻す前に「機内モード起動」を記録し、接続がオフライン、教材3/3保存済み、オフライン4区間の転送量が0であることを確認する。
9. 通信を戻し、「再接続後」を記録する。初回ホーム、3段階、10/10経路、問題メモを確認してJSONを書き出す。
10. JS heapを公開しないSafari等では、OSの強制再読み込み・クラッシュの有無と再現手順を記録し、利用可能ならプラットフォーム側計測を別途添える。

## 証拠の扱い

- JSONには氏名・メールアドレスを入力しない。
- 公開Issueへ添付する前にUser-Agentと端末メモを確認する。
- Gate 3は主要ルートの実タッチ完走と表示確認、Gate 4は公開回線の転送量・初回表示時間と実機ピークメモリの記録が揃って初めて再判定する。
- 現状は実機証拠未採取のため、両ゲートとも「実機待ち」を維持する。

## schema v5と機械検証

診断JSON schema v5には、オンライン6経路と機内モード4経路の確認時刻・性能観測、公開HTTPSの初回ホーム、3つのPWAチェックポイント、問題・再現手順に加え、検証対象の40桁commit、dirty状態、配信base URLを含めます。画面上の「対象ビルド」が公開候補・クリーンであり、検証対象commitと一致することを確認してください。ビルドが変わると古い下書き・性能セッションは復元しません。実際に各画面を操作した後でチェックし、問題がない場合も問題メモへ「問題なし」と明記してください。

```bash
npm run validate:device-check -- path/to/brain-practical-device-check.json --commit <検証対象の40桁SHA>
```

検証は形式、記録日時、viewport、タッチ能力と実タッチ時刻、Service Worker制御、Cache Storage、WebGL、安全域、描画サンプル、10/10完走、問題メモを確認します。さらにGitHub Pagesの正規HTTPS URL、クリーンビルド、40桁commitと `--commit` の一致を必須にします。性能セッションも同じビルド情報でなければ失敗し、localhostや別ホストを拒否します。公開HTTPSの初回ホーム、所定順の10経路、経路URL、接続状態、Canvas表示、横はみ出し0、非負の取得量、オフライン転送量0に加え、3チェックポイントの時系列、オンライン→オフライン→オンライン、単独起動、同一カタログ版、各時点の3教材セット完全保存を検査します。永続保存、JS heap、回線情報をブラウザが記録できなかった場合は警告します。検証成功は「証拠JSONの必要欄が揃った」ことだけを意味し、内容の真正性、画面上の合否、OS/GPUを含む真のピークメモリ、β Gate合格を自動承認しません。

## 2026-08-15 経路証拠の同期回帰

アプリ内の教材移動はHistory APIを使うため、`hashchange` だけを監視すると診断画面で直前の教材ではなく古いホームURLを記録する状態を再現しました。教材状態の変更時に証拠用URLを明示保存し、補助画面を開く履歴へ戻り先を付与しました。390 × 844 pxのWindows Chromiumで、ホームから計測開始、脳表へアプリ内移動、オフライン教材経由で実機診断へ戻る一連の操作を行い、`surface` 観測が `#workspace/surface/lateral` を記録することを確認しました。診断URLを直接読み込んだ場合は、以前の教材URLを誤って引き継がずホームを既定にします。この回帰は実機証拠そのものではありません。

さらに、ホーム未確認のまま脳表上で「断面」を選ぶと、旧実装が `sections.routeHash = #workspace/surface/lateral` を成功扱いで保存する状態を再現しました。現在は10経路を所定順に一つずつ有効化し、直前教材URLが項目の種別と一致し、オンライン／オフライン状態も一致する場合だけ記録します。別画面ではその場で理由を表示してチェックを戻し、途中の確認を解除すると以降の時刻・性能観測も連鎖解除します。ホーム→脳表の正常記録と、ホーム解除後の観測0件を390 × 844 pxで確認しました。
