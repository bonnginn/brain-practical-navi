# 実スマートフォン診断・回帰手順

更新日: 2026-08-15  
対象: β候補 Gate 3（実機操作）・Gate 4（実機性能）

## 目的と判定範囲

`#workspace/device-check` は、端末差を同じJSON形式で記録するための補助画面です。診断値と作業中の下書きは同一オリジンの端末内だけに保存し、画面移動・PWA再起動後も再開できます。自動送信せず、JSONの書き出しを選んだ場合だけローカルファイルを作ります。画面の「下書きを初期化」で端末内記録を初期状態へ戻せます。

この診断だけでは実機ゲート合格になりません。実際のスマートフォンで主要ルートを一周し、タッチ操作、OS文字サイズ、GPU、メモリ、回線、PWA単独起動、オフライン復帰を確認する必要があります。

## 採取する情報

- 記録時刻、任意の端末メモ、現在のパスとハッシュ
- User-Agent、言語、画面・Visual Viewport・DPR・画面方向
- coarse/fine pointer、hover、最大タッチ点数、PWA単独表示
- Service Worker制御、Cache Storage、オンライン状態、取得可能な回線情報
- Storage Estimateの使用量・割当量・永続保存状態
- WebGL / WebGL 2、renderer、最大テクスチャ寸法
- `safe-area-inset-*` の実測値
- 約1秒の静止画面におけるフレーム間隔の中央値・p95・最大値
- `pointerType === "touch"` の実入力記録。マウスとペンは確認済みにしない
- 保存後オンライン、機内モード起動、再接続後の3時点における接続、PWA単独起動、Service Worker制御、教材カタログ版
- 各時点の脳表・断面・局所標本セットについて、期待ファイル数、健全な実キャッシュ数、完了マーカー、セット版

回線情報、JS heap、GPU rendererなど、ブラウザが公開しない値は取得不可のまま記録します。約1秒のフレーム採取は操作中の性能試験や公開回線測定の代替ではありません。

## 実機での手順

1. 公開候補HTTPS URLをiOS SafariまたはAndroid Chromeで開き、脳表・断面・局所標本の3教材セットをWi-Fi上で保存する。
2. PWAをホーム画面へ追加して単独起動する。
3. オフライン教材画面から「実機診断を開始」を開き、端末メモを任意入力する。
4. 「ここを指でタッチ」を指で押し、「タッチ確認済み」になったことを確認する。「端末診断を開始」も実行する。
5. 「保存後・オンライン」を記録し、単独起動、Service Worker制御、教材3/3保存済みを確認する。
6. ホーム、脳表、断面、局所標本、クイズ、編集ツールを実際に操作し、戻って6項目を確認済みにする。下書きは画面移動後も端末内から復元される。
7. 機内モードにしてPWAを完全に閉じて再起動し、脳表、断面、局所標本、クイズをそれぞれ完全再読込・操作して4項目を確認済みにする。
8. 通信を戻す前に診断画面で「機内モード起動」を記録し、接続がオフライン、教材3/3保存済みであることを確認する。
9. 通信を戻し、「再接続後」を記録する。3段階、10/10項目、問題メモを確認してJSONを書き出す。
10. DevTools等を利用できるAndroid実機では、代表ルート操作中のピークJS heapも別途記録する。iOSではOSの強制再読み込み・クラッシュの有無と再現手順を記録する。

## 証拠の扱い

- JSONには氏名・メールアドレスを入力しない。
- 公開Issueへ添付する前にUser-Agentと端末メモを確認する。
- Gate 3は主要ルートの実タッチ完走と表示確認、Gate 4は公開回線の転送量・初回表示時間と実機ピークメモリの記録が揃って初めて再判定する。
- 現状は実機証拠未採取のため、両ゲートとも「実機待ち」を維持する。

## schema v3と機械検証

診断JSON schema v3には、オンライン6経路と機内モード4経路の確認時刻、3つのPWAチェックポイント、問題・再現手順を含めます。実際に各画面を操作した後でチェックし、問題がない場合も問題メモへ「問題なし」と明記してください。

```bash
npm run validate:device-check -- path/to/brain-practical-device-check.json
```

検証は形式、記録日時、viewport、タッチ能力と実タッチ時刻、Service Worker制御、Cache Storage、WebGL、安全域、描画サンプル、10/10完走、問題メモを確認します。さらに3チェックポイントの時系列、オンライン→オフライン→オンライン、単独起動、同一カタログ版、各時点の3教材セット完全保存を必須にします。永続保存、JS heap、回線情報をブラウザが記録できなかった場合は警告します。検証成功は「証拠JSONの必要欄が揃った」ことだけを意味し、内容の真正性、画面上の合否、ピークメモリ、公開回線、β Gate合格を自動承認しません。
