# 脳弓の原画像分節レビュー — 2026-09-07

## 100 µm原画像での接続参照点の再評価

広域直交追補：`render_fornix_native100_series.py --orthogonal` で前後視野をnative Y775–937へ広げ、X669–671/682–684/696–698とZ653–655の12面を作成。`fornix-native100-wide-orthogonal-v1` の全4contact内12面をAIが目視した。report SHA `9995a321f3f84ce79fd226a66f479956d30104478695bd254df0c71eb95c6fb0`。左寄りでは厚い帯状組織の上に薄い層状組織が連なり、中間X682–684では下側の明るい間隙に沿う薄い帯が主体になる。右寄りX696–698では内部の複数の明るい領域が持続する。左右参照点間の中間面は必ずしも標本の解剖学的正中面ではない。

水平面でも左右と上方に連なる形を確認したが、付着部の薄い組織を脳弓・透明中隔・他の付着組織へ分ける根拠としては不足する。従って上方の薄い層を含めた一括polygonや左右の同形コピーを採用しない。これで局所の濃淡・連続性を複数方向で確認したが、脚/柱全長の完成には至っていない。次の採用候補には解剖学的な付着境界を独立に示す資料または明確な原画像所見が必要であり、同じ内部点の微小追加は行わない。他の未解決構造の監査を継続する。広域12面の原画像再decodeとcontact画素一致もテスト対象に追加した。

連続冠状断追補：native Y845–867の全23面を同一視野（X609–758/Z593–716）・同一表示窓で生成し、8contact内の全23面をAIが目視した。`fornix-native100-coronal-series-v1`、report SHA `f0d09147e1e7d38d6d59d00a78dbc00e35ba4c9db2c4a52d5748924aabeddf9d`。全て無着色で、左右参照点周囲と上方付着部を同時に観察した。Y845–850では右内部の細い明るい走行が見え、Y851–859で形を変えながら続く。Y860–867では右側の外寄りの明るい小領域が広くなる。一断面だけの消失・表示ノイズとみなして塗り潰さない。左の帯状組織と上方の薄い組織との接続も全区間で残り、付着部のどこまでを脳弓へ含めるかは未確定。連続する組織の追跡は補強したが、独立した完成境界はまだ定義していない。

`render_fornix_native100_series.py` は固定済み参照reportから視野を定め、原画像を再読取するだけで旧1228点を転写しない。contactへの全23PNGの欠落なし・画素完全一致と、原画像再decodeから無印パネルの再現をテストに追加した。次は上方付着部を矢状・水平断でも広く追い、下方の帯状組織との形態上の違いを検討する。分節の追加採用・公開変更はない。

登録300の左右参照点[322,422,291]/[331,422,291]を公式変換の逆経路でnative100へ対応させ、各点のXYZ中心±1面を確認した。`render_fornix_native100_connection.py`、`work/anatomy-review/fornix-native100-connection-v1` の6PNG・18パネルを全目視。report SHA `9f9978dca5dd6930b4efc51b9a4f11caceb356a2b1d57cc20d6f58d48c7cc229`。同一面が左右の別cropに含まれるため18の異なる切片とは数えない。100 µm原画像SHA `61e6ebbeb0d6876051b9348a68bfe22b733fead04d112c35ff1a29819b67d351`、登録300は `ebf0e88def96476d0a32ddaff6f28e37d7afd125dec724e6d8855b12357c7e86`。出典・decodeは [NATIVE100_BOUNDARY_REVIEW.md](NATIVE100_BOUNDARY_REVIEW.md)。順方向往復誤差最大 `1.0357439013120029e-06 mm` は数値整合性であり登録精度ではない。

左参照点はnative X669–671/Y856–858/Z654–656で帯状組織の内部に位置する。一方、右はX696–698で複数の明るい局所領域とその周囲の組織が分かれて見え、Y854–856では点の近くを細い明るい帯が通る。Z652–654も小さい明るい領域を示す。300 µmでの明るい部分を単なる低解像度表示の問題として無視する根拠は得られず、右参照点からの自動充填は引き続き不採用。血管・腔・標本裂隙のいずれかと断定しない。

100 µmでは内部と周囲の濃淡を追いやすいが、上方付着部・外側組織との分離や脚/柱全長を、この局所18パネルだけで定義できたわけではない。既存1228点は未採用のままで、今回の生成物は参照位置図のみ。次は左右の組織外縁と上方付着部を広い連続断で追う必要がある。現行開発label e98cd406に新しい脳弓ラベルは追加していない。

## 右接続部の隣接矢状断：自動接続の採否

`--right-connection` で原300 X328–335の8連続矢状面を原画像／既存1228点の重ね合わせとして6倍nearest表示し、fornix-right-connection-sagittal-v1の8PNGを全目視。X330では旧体部・下方試案の間に比較的狭い未着色帯が見えるが、X331–333では明るい斜走領域がその周囲を横切る。X334–335ではより薄い帯状組織となる。同一XZ外形の平行移動や隣接点の直線補間ではこの変化を表せない。

判定：右接続部の自動補間案は採用しない。前回の参照点一個だけの問題ではなく、複数矢状面にわたる境界問題である。明るい領域を正常な内部線維・血管腔・標本由来の裂隙のどれかに断定できず、周囲に組織が見えることだけで脳弓の完全maskを定義しない。1228点は画像誘導の内部候補のまま。これ以上の同じ候補の小刻みな追加は保留し、脳弓の全体同定・外縁の追加根拠とともに再評価する。その他の脳室・視覚路・後付けモデルの自律監査は止めない。

## 接続部拡大と左右参照点の直交判定

`--connection-details` の原300 Y419–425、X305–349/Z255–324を座標付き8倍nearest表示し、全7PNGを目視。`fornix-connection-details-v1` は着色なしで、正中の明るい間隙と左右帯状組織の下縁を確認する図である。正中付着部まで一括で脳弓とする根拠は得ていない。

原300参照点[322,422,291]と[331,422,291]を記録し、`render_fornix_bend_references.py --connection` の全2PNG/6直交パネルを目視した（Y422とZ291は左右で同一面）。左点は3面とも帯状組織内に見える。右点は矢状・冠状で明るい小領域に重なり、原値63649。右点を接続maskの内部起点としては採用しない。左の原値54281との差だけを理由に除外するのではなく、直交画像上の局所的な明るい領域が理由。小領域を腔・血管・アーチファクトのいずれかに断定していない。

これは左右で同形の接続polygonを転用できない具体例となった。右はこの領域の周囲を隣接矢状面で追跡し、帯状組織のどちら側に連続が残るかを確かめる必要がある。左も一点の支持から全外縁へ拡張しない。1228点の候補・教材label・meshは変更なし。参照点JSONは診断対象の位置を保持する記録であり、採用点リストではない。

## 体部末端から屈曲部への連続表示を再確認

`render_fornix_draft_orthogonal.py --connection-series` で既存の体部1098点と下方130点を同時に重ね、原300 Y416–434の19連続面を補間なしで生成した。`fornix-connection-series-v1` の7PNGを全目視。report SHA `d43e94c7c4a5bc3bf939e2bfccc2ad31d149451edbfe550885f8c3011e68bcfd`。原画像・点集合は以前と同じで、新しい19切片の取得ではなく、接続判定に使う一貫した連続表示である。

Y421→422でオレンジ領域が急に下方の小さい十字へ変わるのは、体部内部案から別設計の屈曲部内部案へ切り替わるため。原画像ではこの位置の帯状組織そのものの突然の消失は見えない。したがって、前回の矢状図で見えた「候補間の間隙」を組織の断裂と解釈しない。ただし上方の薄い正中組織との付着、外側の隣接組織、屈曲部の全外縁をこの連続表示だけで分離したともいえない。現1228点の自動補間・一括膨張は行わず未採用を維持。

次に必要なのは、Y419–425の外縁を座標付き拡大で追い、矢状面と交差確認すること。内部の点数を増やすこと自体を目的にしない。さらに前後の脚・柱と、前交連を含む位置関係が必要で、今回の局所レビューを脳弓全体の完成とは扱わない。

レンダラーではmodeのbool `combined` を接触画像のPILオブジェクトで上書きしていた点も修正した。将来の既定モードで第2軸の選択面が変わる潜在不具合を除いたもので、過去のPNGは変更なし。保存済みfornix-draft-orthogonal-v1のreportを確認するとX315–337・Z290–300が記録されており、その記録を今回の不具合から一律に無効と判断しない。

## 体部・屈曲部の統合表示と採用判定

`render_fornix_draft_orthogonal.py --combined` は固定SHAの体部1098点と屈曲部130点を合わせ、1228点を補間なしで表示した。`fornix-combined-orthogonal-v1` の全4PNG（X320/321/322/329/330/331、Z284/287/290/293の10面）を目視。屈曲部の小試案は下方の組織内に位置するが、体部案との間には未分節の間隙が残る。特に左側は内部案同士が離れており、この2案だけでは連続した脳弓モデルにならない。右側の近接も接続の解剖学的証明ではない。

現時点の採用判定は**いずれも未採用**。部分的な内部点集合を完成ラベルへ昇格させない。体部外縁・接続部・脚/柱の全長は未完了として保持し、次の脳弓作業では内部点の微小追加を繰り返すより、接続部の境界を追える画像根拠を優先する。全目標は継続し、並行して未点検の側脳室境界なども対象に戻す。

## 屈曲部下方の内部試案

`render_fornix_bend_core_draft.py` でY422–434の13面について、下縁参照点より上方の小さな内部試案を作成した。各面左右5原画素ずつ、計130原300点。これは境界polygonの完成品ではなく、屈曲部下方で連続して追える内部の限定試案である。旧体部案との間を自動で埋めたり、同じ形を外縁まで拡大したりしていない。

`fornix-bend-core-draft-v1` の全5contact（前後面込みY421–435）を目視。今回の小範囲は下方の明るい腔・正中裂隙・内部の明るい小領域を大きく跨いでいない。原値49103–59735は参考記録であり、脳弓としての組織同定や完全な境界保証ではない。130点全座標と各画像SHAをreportへ保存した。

未完了：この試案の実点集合の直交確認、体部内部案との間の原画像に基づく接続、外縁・脚・柱全体の定義。教材格子への採用は未実施。小さい内部案を追加しただけで「脳弓の分節完成」としない。

## 屈曲部の座標参照点と直交照合

目盛付き原画像Y422/428/434を生成し全3画像を目視（fornix-bend-coordinates-v1）。下方の組織を追う参照点6個を `segmentation-patches/review/fornix-bend-reference-points-2026-09-07.json` に記録した。いずれも原300整数XYZであり、教材格子の追加voxelではない。

`render_fornix_bend_references.py` により各点の矢状/冠状/水平図を作成し、全6PNG・18パネルを目視（同一平面の別参照点表示を含むため18新規断面とは数えない）。Y428/434の参照点は下方の帯状組織へ対応する。一方、Y422の[321,422,284]と[330,422,286]は矢状面でも下縁に近い。単なる内部代表点として無条件に拡張するのは不適切で、将来のregion-growing起点としては採用しない。原値は51126–57444だったが、値だけで内部/境界を確定しない。

新しい参照点群は位置追跡の記録に留める。既存体部案の末端と下方参照点の間を線で結んだだけのmaskは作らない。次の下書きでは下縁から余裕を取り、内部の明るい領域を跨がない範囲を断面ごとに定義する必要がある。教材label・mesh変更なし。

## 屈曲側Y421–435の連続確認

`render_fornix_draft_orthogonal.py --bend-series` でY421–435の15連続面を生成し、`fornix-bend-series-v1` 全5PNGを目視。既存内部案の末端Y421だけにオレンジ表示があり、後続を自動補間していない。Y422–435でも左右の細長い組織は途切れず見えるが、上方では薄い正中組織に連なり、外側には小さな隣接組織が接する。見えている組織全体を脳弓へ一括分類できる根拠にはならない。

下方の組織端が徐々に移り、片側では内部に明るい小領域が見える。単一閾値や同一polygonの反復では、これらを区別できない。今回、追跡可能な組織の連続性は補強したが、脳弓と上方付着部の分離線は確定していない。次は座標目盛付きで下方内部の参照点を取り、同位置を矢状/水平へ戻してから新しい内部案を作る。既存1098点の拡大採用は未実施。

## 前後を広げた位置関係の確認

`render_fornix_draft_orthogonal.py --wide` で原300 Y340–474/Z230–344まで広げ、X318/321/327/330/333の5矢状面を個別目視した（fornix-draft-wide-sagittal-v1）。候補は脳梁下の帯状組織の短い内部部分で、組織は候補両端を越えて続く。傾斜して下降する側を同じ高さのpolygonで延長するのは不適切。正中X327では候補を置いておらず、上方付着組織との連続だけを理由に正中を埋めない。

`--wide-coronal` によるY392/398/404/422/428/434/440/446の8冠状面も全て目視（fornix-draft-wide-coronal-v1）。Y422以降では左右の細長い断面が下方へ伸び、その後周辺組織との接触が増える。これは脚/柱の命名を確定した結果ではない。6格子間隔の位置関係調査であり、間の連続断は未確認。次の追跡区間は既確認Y421に接続するY422–434の連続面とする。旧内側polygonの平行移動だけでこの屈曲部を採用しない。

全13画像で固定1098点以外の着色追加なし。製品label・mesh・公開サイト変更なし。

## 延長案の実点集合による直交確認

`render_fornix_draft_orthogonal.py` は延長v2の固定reportから1098点を読み、原300画像のX315–337（23面）、Z290–300（11面）へ実点集合を投影する。`fornix-draft-orthogonal-v1` の全12PNG・34面を目視した。旧脳梁除外領域ではなく今回の内部案そのものをオレンジ表示し、左右に同じ原画像の無着色版を添えた。

矢状面では上方の明るい腔と下方の明るい裂隙の間にある帯状組織内に候補が位置し、水平面でも正中を跨いだ一括充填にはなっていない。一方、矢状面の候補は前後端が直線的で、原組織の傾斜・曲がりを外縁全体として追えていない。これは内部下書きの切り止めであり、解剖学的な脳弓端ではない。明らかな腔への大きな突出は今回の図では認めないが、部分体積を含む外縁の確定・体部全長・脚/柱との連続性は証明していない。

次は現在の小範囲を完成品にせず、より広い原画像で前後へ帯状組織を追い、体部から脚・柱への接続位置を調べる。75 app voxelの幾何包含結果だけで新ラベルを採用しない。原画像・製品label・meshは変更なし。

## 延長内部案の教材格子対応

`audit_fornix_draft_grid.py --body-extension` で延長v2 reportのSHA `c4dc1bc17eaf9be89ae044803f3e38300eb02833294011450785f1150cb5d1ef` と1098点を固定検査した。中心対応は301 app voxel、有限セル全体が内部案に収まるものは75、既存ラベル中心競合は0。残226点は内部案の範囲外を含むので、中心対応だけでは採用しない。75点も幾何包含の結果であって解剖学的確定ではない。

結果は `work/anatomy-review/fornix-draft-grid-body-extension-v1/report.json`。旧183点の検査結果は上書きせず保存。有限セル列挙テスト3/3成功。脳弓体内部案の実点集合の直交重ね合わせ、体部外縁・脚・柱の追跡は未完了。製品ラベル・mesh・公開サイトは変更していない。

## 体部内部下書きの前後延長と断面別調整

`render_fornix_core_draft.py --extend-body` で原300Y404–421へ内部案を延長し、Y403–422（前後面込み20面）の全7contact画像を目視した。各面61点、計1098点。Y419では下方1原格子、Y420–421では2原格子へ内部案を移したが、これは原画像で見た内部範囲への下書き調整であり、外縁の正解曲面ではない。単一の平板や元2160点を脳弓として採用していない。

延長v1で左初期側の内縁が裂隙にかかり、原値65535は[323,407,296]。65000以上の10点はY404–408に分布。`--refine-extension` で左案をY404–407はX負方向2原格子、Y408は1原格子だけ調整し、別v2として保存した。v2の変更5面をcontact-0/1で再目視した。他の面はPNG SHA比較で不変。v2=1098点、既存appラベル中心照会は全0、原値51888–65422。最大値の低下は解剖学的検証の代わりにしない。辺縁の部分体積はなお確認が必要。

`fornix-core-draft-body-extension-v1`/`v2` に全polygon・点集合・PNG SHA保存。次は延長v2の直交差分と有限app支持を検査し、支持不足を下書き外と組織外に区別する。1098原点は脳弓体内部の案であり、全体/脚/柱完成ではない。教材ラベル変更なし。

## 原300下書きとapp500の有限voxel対応

`audit_fornix_draft_grid.py` はv2 reportのSHA bed75030…を固定し、183原300点からapp500の中心丸め候補を計算。81 app voxelに対応し、既存ラベルとの中心競合は0。ただし原300画素を単位セルとみなし、各app voxelが正の体積で交わる全原セルを列挙すると、全支持範囲が下書き内に収まるのは8個だけだった。残73個は有限範囲が下書きを超えるため、中心丸めだけによる採用はしない。

`work/anatomy-review/fornix-draft-grid-v1/report.json` に81点のapp XYZ・原中心・交差セル数・下書き外数・濃度範囲・既存labelを保存。これは小さい3面下書きに対する幾何包含検査であり、8個だけを脳弓完成とする判断ではない。原画像の解剖学的外縁を下書きが覆っていないことも保持する。次は安全に対応する点の直交表示を確認し、体部の前後方向へ原画像で確かめた下書きを延ばす。膨張や閾値緩和による一括拡大はしない。

有限セル列挙は境界が接するだけの隣接セルを含めない。専用テスト3/3成功（完全一致セル、3軸の面横断、非有限/不正範囲拒否）。原画像・製品label・meshは不変。

## 下書きのラスタ化と内縁の改訂

`render_fornix_core_draft.py` で固定JSON（SHA9a82db81…）を原300のXZ画素へラスタ化。v1は219点で、原300中心からapp500へ照会した既存ラベルは全点0。これはappへの採用可能性や境界を保証せず、重複検査のみである。Y410–414の5PNGを目視し、左側の内縁の一部が白い裂隙にかかることを確認。原値65535は[324,411,296]、65000以上の他3点は[323,411,294]/[324,411,297]/[324,412,296]だった。

左のpolygonを原画像に基づき内側から縮め、`--trim-inner-edge` で別のv2へ保存した。旧v1は上書きしない。左polygonはXZ[[318,296],[320,299],[322,299],[323,296],[321,294],[319,294]]、右は不変。これは閾値一括除外ではなく、内縁の形の改訂。v2の183点、原値53226–63102、既存label中心照会は全0。v2全5PNGも個別目視済みで、冠状断では左右の組織内部に収まる小範囲として確認した。

成果物 `fornix-core-draft-overlay-v1`/`v2` のreport.jsonに全点、使用polygon、入力SHA、画像SHAを保存。**まだ原300の内部下書き**で、体部外縁・全長を覆わず、app500のvoxel支持範囲と直交差分を未検証。原画素の最大値低下を解剖学的正確性の証明とはしない。次はv2の実際の点集合を直交面へ投影し、app500へ移した場合の支持範囲・競合を調べる。製品変更なし。

## 参照点の直交照合と内部下書き

`--seed-orthogonal` によりX320–322/332–334、Z295–297/300–302の12面を生成し、fornix-native300-seed-orthogonal-v1全4PNGを目視。矢状断では参照点付近が脳梁より下の弧状組織へ連なり、水平断では正中の間隙を挟んだ左右の帯状組織を確認した。旧locatorは一部しか被覆せず、外縁の正解maskではない。これで脚/柱全長や他の線維との分離を完了したとはしない。

座標拡大図Y411–413の左右参照点周辺に、体部内部の限定polygon下書きを作成した：`segmentation-patches/review/fornix-body-core-draft-2026-09-07.json`。原300整数XZ、各面2 polygon。旧2160点の転写ではなく、正中X325–329付近の裂隙と上方付着部を含めない内部案。外縁全体を描いたものではない。同じpolygonを3面に記した有限下書きで、ラスタ化後の原画像照合は次段階。採用・app格子変換・publicラベル変更は未実施。次はこの固定下書きの原画像重ね合わせと前後面を確認する。

## 座標付き拡大と初期参照点

`--coordinate-details` を追加し、登録300のXYZ[310,400,280]–[344,426,320]を拡大。Y404–406/411–413/419–421の全3PNG9面を個別目視した（既確認断面の拡大であり新規切片ではない）。`fornix-native300-coordinate-details-v1` に原画像と旧locatorを並べ、原格子X/Zの目盛を付けた。Y419–421の正中の白い裂隙に旧locatorの上向き突出が重なるため、旧2160点の全体流用は不適切という判断を具体化した。

原画像での追跡起点として[321,412,296]と[333,412,296]、埋めない裂隙の参照点として[326,420,296]を記録（すべて登録300の整数XYZでありapp500座標ではない）。`segmentation-patches/review/fornix-body-localization-2026-09-07.json`。これらは境界polygon・分節・採用ラベルではなく、次の直交断追跡の参照点。点からの無制限region-growingや箱塗りを許可しない。次はこの組織内部点から矢状/水平で連続を追い、近接する付着部を含まない候補を定義する。

## 体部付近の連続冠状断（後続）

`review_fornix_native300.py --body-series` で登録300のY404–421を切れ目なく生成。`fornix-native300-body-series-v1` の全6PNG18面を個別目視した。初期Y411–413と重複するため、新しい異なる面は15。現d4295e7c…のSHAも再確認した。

左右の組織断面と上方の薄い正中組織との接続は、この範囲を通して続く。Y404–407の緑は左右に分かれ、Y408以降では上部でつながる部分を含む。Y418–421では旧locatorが薄い正中組織の方向へ突出する。これは旧maskの形態の記録であり、その突出を脳弓の解剖境界とはみなさない。周辺の小さい組織片と接する部分もあり、濃淡だけで体部全幅を指定できたわけではない。

判断：旧2160点の全体採用は引き続き不採用。まず体部中央と上方付着部を切り分けて局所候補を作る必要がある。脚/柱全長の確認は未完了。今回の18面だけで終了とはしない。public label・模式mesh・公開サイトは変更していない。

## 現状

現開発label d4295e7c…には独立脳弓IDがない。`build_specimen_blocks.py` のfornixは固定polyline/radiusによる模式補助で、原画像から抽出した分節ではない。手元のPD25/BigBrain手動ラベルCSVは1–22の核・海馬等で、脳弓を収載していない。模式部品を正解maskとして転写しない。

以前のID30下方成分2160点は脳梁から除外済み（CALLOSUM_INFERIOR_REPAIR.md）。脳弓との走行の整合はあるが、その除外判定は脳弓への再分類を意味しない。

## 登録300µmの初期照合

`review_fornix_native300.py` は旧8cc65e…の固定成分4を、2160点と全index SHA6a4b7677…で照合してlocatorとして利用する。現d4295e7c…の脳梁輪郭とともに、登録300µm原画像ebf0e88d…へ正しいworld座標で最近傍投影した。歴史的locatorは緑、現脳梁は赤。緑は未採用脳弓maskではなく、過去の除外領域にすぎない。

`work/anatomy-review/fornix-native300-initial-v1` の全3PNG9面を個別目視した。X326–328、Y411–413、Z289–291。画像・入力SHAとcropはreport.json。raw/overlayを並べ、画像やラベル本体は変更していない。

- 矢状断では脳梁下方の弧状組織と前方下降部を追う手がかりがある。緑の幅と微細な原画像境界は完全には一致せず、全2160点の再分類を承認しない。
- 冠状断では上方脳梁とその下の細い正中連続部、その下方の左右に分かれる組織断面を確認。透明中隔との接続を含むため、上方まで一括して脳弓としない。
- 水平断では正中付近の左右の組織と間隙が見えるが、脳弓脚から海馬への連続や柱の終点は、この初期9面では確認していない。

次はこのlocatorの前後連続をたどり、体・脚・柱の範囲と透明中隔/脳梁との境界を別々に評価する。旧maskのままの採用、白質輝度のみでの抽出、模式polylineの原画像転写はしない。独立分節は未完了で、今回新規ラベルや公開変更なし。

## 外部資料の位置づけ

[BigBrain公式Maps & Models](https://bigbrainproject.org/maps-and-models.html)を再確認したが、このページは脳弓の取得可能な独立volumeを示すものではない。[Jonesらの2020年発表](https://bigbrainproject.org/docs/4th-bb-workshop/20-06-26-BigBrainWorkshop-Jones.pdf)は既調査資料であり、新規発見とは数えない。脳弓を含む同一標本の周囲白質研究だが、手元に再利用可能な脳弓ラベルを取得済みではない（HYPOTHALAMUS_EXTERNAL_REFERENCE_REVIEW.md）。今回web経由のPDF取得はサイズ制限で失敗し、再読したとは扱わない。既存の100µm視床下部ROIは柱の局所確認の候補であり、脳弓全体を含むと仮定しない。
