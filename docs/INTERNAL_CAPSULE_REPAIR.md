# 内包候補の修復調査 — 2026-09-06

## 現行e98cd406への再照合（2026-09-07）

### 内包の曲がりを含む広域水平断

後続の直交照合：同scriptの `--orthogonal` で、登録300 X291–293 / X364–366（app X175/219付近）とY449–451（app Y270付近）、上下はapp Z130–190へ広げた9比較を生成し、全9PNGをAI目視した。`capsule-turn-orthogonal300-v1/report.json` SHA `f6d9fb81ea76acd835bbd5d604de046c1899d12d85a3bec1da4fc535b59daf68`。矢状面の明るい帯は尾状核下方・視床前方を通り、下方の濃い領域との境で現輪郭に不整がある。冠状面では左右とも核に挟まれた帯へ概ね対応し、帯内にも細い暗色の筋がある。筋を灰白質や別の線維路と確定して削除しない。今回のcrop外までの全終端・全前後範囲は未確認で、膝の厳密な境界や上端は確定しない。追加採用なし。原画素・全9面の独立最近傍label投影・表示方向・入力不変を含め、関連テスト3/3成功。

`render_capsule_turn_context.py` と `work/anatomy-review/capsule-turn-context300-v1/report.json` に、登録300 µm原画像Z249–251、256–258、263–265の9比較を保存し、全9枚をAI目視した。3組の隣接3面であり、Z249–265の全連続面を確認したわけではない。原画像SHA `ebf0e88def96476d0a32ddaff6f28e37d7afd125dec724e6d8855b12357c7e86`、現行ラベルSHA `e98cd4060d735c732a5fd75030be2f701f57fe91b6cd5b9a12c65e1cb68b37e3`。左は無着色原画像、右は尾状核（珊瑚色）、被殻（水色）、淡蒼球全体（金）、視床（紫）、内包（緑）の輪郭。淡蒼球内外節はこの図では合成表示している。

前方の尾状核と被殻の間から、後方の視床とレンズ核の間へ明るい帯が曲がって続く。現行内包は概ねこの帯を収載する一方、核に接する輪郭には階段状の部分があり、原画像の細い明暗の帯すべてに一致するわけではない。Z263–265で淡蒼球断面が縮小しても明るい帯は続く。これを座標だけで前脚・膝・後脚へ分けたり、明るい領域すべてを内包へ拡張したりする根拠にはしない。特にcropの前後端は途切れており、全体の終端や放線冠との移行は検証できていない。

今回新しく確定できた削除・追加voxelはなく、label/meshは変更しない。次の細分化には移行部を含む直交断と範囲定義が必要。図の科学用affine、選択した9面、原画像再読み込み・濃度変換と無着色パネルの画素一致を `tests/test_capsule_turn_context.py` で検査し、1/1成功した（解剖学的正確性のテストではない）。

前方境界のnative100照合：`render_fornix_native100_connection.py --capsule` でapp [155,299,159]/[235,299,159]が現行ID31/32であることを検査し、科学用affineと公式逆変換で原画像へ対応させた。`capsule-native100-anterior-v1` の6PNG・18直交隣接パネルを全目視。report SHA `89febc26fd748d43941cf9162b30fd908bf0e155d7928895aff3483a95aa4ed9`。原画像SHA `61e6ebbeb0d6876051b9348a68bfe22b733fead04d112c35ff1a29819b67d351`、window40000–65535、per-Y decodeは [NATIVE100_BOUNDARY_REVIEW.md](NATIVE100_BOUNDARY_REVIEW.md)。最大往復計算誤差2.16758e-6 mmは解剖学的登録精度ではない。

左点はnative X502–504/Y1018–1020/Z591–593で明るい帯状組織内にあり、周囲に暗い帯が交互に入り込む。右点はX890–892/Y1019–1021/Z617–619で同様の明暗が入り組む境界寄りにあり、左と同じ外形の転写はできない。水平面では明るい帯が連続している。これら2点を含む旧除外候補を「灰白質の取り込み」として一括削除する根拠は得られない。ただし点は有限500 µm voxel全体や周囲全境界の支持を保証せず、前脚/膝/後脚の細分化も今回完成していない。新しいlabel/meshの採用はなし。原画素の再decode・無印パネル一致・科学用affineによる参照座標をテストに追加した。

`audit_capsule_after_registration.py` に明示input/SHAの組を受け取る読み取り専用経路を追加した。既定の7ebed144段階の検査は保持し、旧証拠を上書きしない。`work/anatomy-review/capsule-after-outer40-v1.json`（SHA `ef8ce25c18c08ef1f77b6a00d8e784cbf183b4e6a5a44025df5571c3c2cccc73`）で、旧除外候補1616点は現行でも左711/右684点が内包、左145/右76点が手動核ラベルであることを再確認。核へ修正済みの221点を旧削除案で消さない。

さらに旧7ebe復元fixtureと現行の内包mask全体を独立に比較し、ID31は56826点、ID32は53609点、両maskの差0点だった。したがって以前の全107比較の対象内包境界そのものは現行にも残り、脳室40点修正で内包境界まで改善したとは報告しない。これは境界が正しい証明ではなく、再確認対象の版照合である。

対象指定・誤SHA拒否・既定の旧版検査・入力不変の3テスト成功。追加の内包削除/細分ラベルは今回なし。次に必要なのは既存境界の前下方の範囲、前脚/膝/後脚の移行、上方で放線冠へ移る範囲の画像根拠であり、1616点の旧形態処理差分の一括再適用ではない。

## 標準生成経路との分離・全ラベル再現確認

除外復元は未採用なので、`main` は `preserve_nuclear_exclusions=False` を明記して採用済みの旧候補を再現する。実験用関数の既定値はTrueのまま。元アトラス由来の変更を先に入れると、承認済み乳頭体パッチの入力SHAが変わって生成が失敗するためであり、SHAガードは緩めない。将来採用する場合は旧生成を維持してレビュー済みの限定差分を新しい後段パッチにする。

`audit_capsule_morphology.py` のソース文字列置換／execは撤去し、この明示引数で旧新を実行するようにした。再実測は左右143/143 atlas voxel、現行856/760 voxelで変化なし。未採用差分はJSON内容で一致（改行形式によるファイルSHA差あり）。合成テスト4/4に旧動作とmainの固定モードを追加。

さらに `audit_practical_reconstruction.py` で、配布rawから空間判定、現行1–22から保存済み手動ラベル、公式atlas/WMからその他ラベルを、実際のmainの生成文を用いて再構築した。乳頭体・脳室の2承認パッチも入力SHAガードを通して適用し、**現行volumeとの差0 voxel、raw SHA b1105fd3a11fab27d3b1bac60d4d989386e4ef49a41151f5b684d984f72aaaa9**を確認した。原NIfTIの読み込み／変換そのものは再実行していないので、全NIfTIビルド完了とは言わない。証拠 `work/anatomy-review/practical-reconstruction-v1.json`。公開資産は未変更。

## 続報：実アトラスでの再現と差分点検

元データを公式TemplateFlowの配布からローカルに再取得し、GitHub git-annexのsize/MD5と一致を確認した。

- `tpl-MNI152NLin2009cSym_res-1_atlas-CerebrA_dseg.nii.gz`: MD5 `7b69ad2478c6be7de12bb5b254b4cb7c`、564,070 bytes。
- `tpl-MNI152NLin2009cSym_res-1_label-WM_probseg.nii.gz`: MD5 `e5f636592b9c3a3eea4660ebc987a385`、5,427,336 bytes。
- 配布: `https://templateflow.s3.amazonaws.com/tpl-MNI152NLin2009cSym/` に上記ファイル名を付加。照合元 [公式リポジトリ](https://github.com/templateflow/tpl-MNI152NLin2009cSym)。SHA-256は生成reportにも保存。既存のMNIライセンス条件を維持し、取得データは `work/segmentation-source-review/` のみ。
- ローカル分離依存: `work/segmentation-deps/` に scipy 1.18.1 / nibabel 5.4.2 / numpy 2.5.2。アプリの依存・lockfileを変更していない。

`scripts/audit_capsule_morphology.py` は実SciPyで元関数と修復関数を計算し、保存済みBigBrain affineへ最近傍対応させた。元関数は修正行だけを外して再現する。**現行31/32ラベルの再現範囲外voxelは左右とも0**。ただし他ラベルにより上書きされる前の候補なので、この包含一致を全パイプラインの完全再現とは言わない。

| 側 | 元アトラスで除外復元 | BigBrain格子へ対応 | 現行内包の変更候補 |
| --- | --- | --- | --- |
| 左31 | 143 | 1,144 | 856 |
| 右32 | 143 | 1,144 | 760 |

`work/anatomy-review/capsule-morphology-v1/report.json` に全候補座標、入力SHA、格子metadata SHA、各画像SHAを記録。strict差分 `segmentation-patches/review/capsule-morphology-candidate-2026-09-06.json` は **low / unreviewed** で保存した。配布volumeは未変更。

`scripts/render_capsule_change_contacts.py` で全影響Z87面＋左右各X5/Y5の20面＝107原画像・差分対を9シートへまとめ、AIが全9枚を開いて点検した。最初の3枚は1画素/voxel版、その後は最近傍2倍版で点検（現在保存される全シートは2倍版）。赤が現行内包の輪郭、黄色が除外候補。

差分は核との境界に多いが、Z139–141、Z158–160などの前脚沿いの細い明るい線維帯にも及ぶ。矢状断でも明るい帯の縁に小さな削除が散在する。**アトラス核除外という実装規約の復元はできたが、全1,616 voxelの削除がこの標本で正しいという所見ではない**。一括採用しない。前下方の帽状領域全体を除去する修正でもない。原画像で曖昧な境界を不用意に消すより、この候補を保留して次の対象の作業を進める。

追加で生成関数に「軸がworld x/y/zと整列した1 mm格子」の入力チェックを実装した。既存計算はvoxel距離をmmとして用い、affine対角成分で座標を計算するため、異方性・回転／軸交換・shearを黙って処理しない。実データの格子はこの条件を満たす。合成異常系テストを追加。

再実行は `PYTHONPATH` に `work/segmentation-deps` を設定し、`audit_capsule_morphology.py`、次に `render_capsule_change_contacts.py`。全体のNIfTI生成や、承認済み乳頭体・脳室パッチの入力SHAの変更は行わない。

## 今回の到達点

**生成処理の除外条件を修正した。配布volumeの境界修復や前脚・膝・後脚の分節完了ではない。**

`atlas_white_matter_candidates` は尾状核・視床側（CerebrA IDs 100/49/91/40）、被殻・淡蒼球側（72/21/78/27）を内包候補から除外した後、binary closingを実行していた。Closingは除外済みの穴を再充填し得るため、closing後にも `~medial & ~lateral` を適用した。白質閾値、距離制約、左右分割、手動核ラベルの優先順位は変更していない。

この変更は「除外した核を後段で再び候補に含めない」という生成規約の修復であり、現行内包の前下方の帽状部分がこの不具合で生じたと証明するものではない。後段では手動核ラベルの既存voxelを上書きしないが、手動核とCerebrAの境界は一致するとは限らないため、生成段階の除外条件も維持する必要がある。

## 再点検した画像

入力ラベルSHA: `b75a24903ec08526b3e7f08df9efc8cee15af80d86bb96a821260913a2b176f3`。
原画像SHA: `c4b69975f0dece2512adf3bcae690226492cfa66ded38380b3b94aa8dba52746`。

既存の証拠画像を今回再び開いてAIが目視した（新規撮影ではない）:

- `work/anatomy-review/extent-followup-v1/capsule-inferior-x-0.png`: X153/159/165
- 同 `capsule-inferior-x-1.png`: X226/232/238
- 同 `capsule-inferior-y-2.png`: Y303/309/315
- `work/anatomy-review/complete-z-v1/ids-31-32-z-124.png`: Z124–131、原画像と輪郭

矢状断で内包候補は核の上方を回り込む明るい線維域に沿う部分がある。前下方の冠状断・水平断で灰白質と線維が入り組むため、前回の「灰白質側に見える」という所見だけで帽状領域全部を消す根拠は不足する。左右を対称化せず、今回は削除差分を作らない。

内包前脚は尾状核頭とレンズ核の間、後脚は視床とレンズ核の間、膝は両者の移行部として追う（[UTHealth Neuroanatomy Online, Lab 10](https://nba.uth.tmc.edu/neuroanatomy/L10/Lab10p01_index.html)）。この一般的位置関係は確認したが、BigBrainのこの領域の全voxelを同定する根拠には置き換えない。

## テストと残る作業

- `tests/test_capsule_exclusion.py`: 2テスト成功。小さな合成穴がclosingで埋まることと、8種類の核ID×左右16条件で除外が最終候補まで維持されることを、実際の生成関数へ注入したNumPyの6近傍closingで確認。実SciPy／実アトラスの再生成テストではない。
- 既存ventricle-adoptionテスト3/3成功。配布assets変更なし。
- 実アトラス入力からの再生成・変更voxel集計・三方向の差分レビューは未実施。承認パッチの入力SHAガードを緩めず、再生成後の変更が旧パッチと競合しないことを確認してから採用する。
- 次に必要なのは、元アトラス上のclosing前／後／除外復元後の差分をBigBrainへ対応させること。今回のコード修正だけで配布版に改善が反映されたと記載しない。

## 2026-09-06 後続：模式神経・血管と内包の再評価

現行7ebed144…で古い1,616点の候補を再分類した。左856点のうち145点、右760点のうち76点は既に手動核ラベルになった（被殻・淡蒼球・視床）。残る711/684点のみが内包だが、その削除の正しさは確定しない。scripts/audit_capsule_after_registration.py と work/anatomy-review/capsule-after-registration-v1.json に記録。古い差分を現行へ自動再適用しない。

## 現行ラベルの画像再点検（2026-09-06 後続）

`render_capsule_current_review.py` で残る左右711/684点だけを黄色に示し、現行内包を赤、登録済み手動核を水色で重ねた。原画像は左に無着色で並置。全影響水平面87比較＋左右各5矢状・5冠状断の20比較、計107比較・18シートを作成し、AIが全18枚を実際に開いて確認した。すべての点について水平断で比較できるが、すべてのX/Y面を確認したという意味ではない。

- 左 Z139–141、Z158–160 の黄色は前脚側の明るい線維帯に沿う。Z148–153の後脚側にも核との薄い境界に候補が残る。削除すべき灰白質を一括して取り込んでいるとは判断できない。
- 右 Z149–160でも同様に前脚側とレンズ核側の縁に候補があり、特にZ159–160は明るい帯を削るおそれがある。左右の範囲・本数を機械的に揃えない。
- 左 X155/160/169、右 X223/231/236では、黄色が核周囲の明るい線維域やその縁を通る。冠状断でも薄い境界点として残り、灰白質／白質の全点判定を与えない。
- 前下方の帽状領域の収載範囲と、内包から放線冠などへの移行は別の未解決問題。今回の残存削除点の点検で内包全体の正確性を保証しない。

**判断：この旧アトラス除外差分を現行版へ一括採用しない。** 221点は既に核として修正済みで、残る1,395点にも温存すべき可能性のある線維帯が含まれる。今回、独立に採用できる削除点を確定できなかった。判断根拠不足を「正常」「専門家確認済み」に読み替えない。ラベル・mesh・公開サイトはこの再点検では変更していない。

保存先: `work/anatomy-review/capsule-current-review-v2/current-00.png`～`current-17.png`。入力SHA、各図SHA、断面位置、crop、確認点数は `segmentation-patches/review/capsule-current-image-review-2026-09-06.json`。最初のv1は長い見出しが隣パネルへ重なったため、短い見出しのv2を作り直し、全18枚を改めて確認した。原画像・着色領域は同じ入力から生成した。

再点検コードのテストは、現行核を候補から外すこと、Fortran XYZの位置を保持すること、入力配列不変を確認する。これらの機械検査を解剖学的採否の証明とは扱わない。

## 300 µm原画像で前方の核境界を追加確認

現行ラベル86e3b22d…で旧候補座標を読み直し、左右の実在する残存内包点
app XYZ [155,299,159] / [235,299,159] を中心に9 mm半径で照合した。
`render_medullary_native300.py --capsule` は既存の変換済み300 µm原画像を使い、
500 µmラベルを最近傍で投影する。画像を500 µmから拡大したものでも、
内包ラベルを300 µmへ高精細化したものでもない。

三方向それぞれ隣接3面、左右計18面6枚を全個別目視した。
`work/anatomy-review/capsule-native300-v1/report.json` に原画像・現行ラベル・
各画像SHA、中心とcropとnative indexを保存した。赤は内包、水色は登録済み核、
左パネルは無着色原画像。旧削除候補そのものを黄色表示した図ではない。

水平断では核の間の明るい帯が連続する一方、冠状断・矢状断では核の縁で
明暗の細い帯が入り組み、現行輪郭がそれらを横切る。ここを単一の灰白質塊と
見なして丸ごと削除する根拠はなく、旧候補1,395点の一括削除は引き続き不採用。
今回の2局所だけで前脚・膝・後脚全体や上方終端を確認済みとはしない。
新たに安全な削除範囲を確定できなかったため、配布volume/meshは変更していない。
