# 断面ページの脳室3D同期（2026-09-07、開発版のみ）

## 最新：側脳室の34点補完（統合検証完了）

全559/559（work/lateral-crop34-full-node-v1.log、164663ms）、型検査、適用後導入3/3成功。
通常本番build成功後、右4345を更新し水平断72/76＋3D、側脳室/脈絡叢/内側側頭葉3ブロックを目視。
確認前の水平断45・脳室系3/3・3D1面・配分40へ戻した。既存の狭幅見出し省略/サイドバー密度は残る。
これは該当表示の描画確認であり、全画面QAやブラウザ配信内容のSHA取得ではない。以下は直前工程の履歴。

`install_lateral_crop34.py --apply`で20ファイルの採用計画を実行。旧3849ラベル/5旧meshを保存。
現圧縮SHA `a2ceb2649ec0950eb7ba0620f38db9f8fc83293ad46bb2b7fcce82091360a6c5`、
raw `3b21494d28710dd1f88b452e4d0385fe1b521767ac3cdbe96409090d0a49d1e6`、ID24=63849。
側脳室/全体系meshは各34点増、第三/第四不変。全55ブロックmaskのうち5変更/50不変。
画像根拠は132原300面とnative100代表3点27面。専門家レビューではない。
採用記録はlateral-crop34-adoption-2026-09-07.json。新旧採用テスト10/10成功、全Node実行中。
実ブラウザ/右更新は統合検証後。以下は各時点の履歴であり、現在の最新版を指さない。

## 最新：側脳室の21点補完（統合検証完了）

`install_lateral_cavity21.py --apply` で旧a512と6旧ブロックmeshを保持して採用。
現圧縮SHA `3849b1bd3c9ccf6d68b8864644c7ac784cba00dceaa006ec4be329f3d217fa29`、
raw `932c1924688c3d4e041868567bd1ce64c61e8acc61abb7a2904a80c0bb6e9819`、ID24=63815。
断面側脳室/全脳室meshは21点増、第三/第四不変。影響する6ブロック部品を同期。
全55maskの比較・増減内訳・全3軸78原300面＋native100代表2点18面の根拠は
LATERAL_VENTRICLE_FRINGE_REVIEW.mdを参照。採用記録はlateral-cavity21-adoption-2026-09-07.json。
新旧採用9/9・適用後導入3/3・型検査成功。全Nodeはlateral-cavity21-full-node-v1.logで558/558成功（172283ms）。
通常本番build成功。右4345をreloadし、水平断45/70/72＋透過3D、側脳室・脈絡叢・内側側頭葉3ブロックを目視。
最後は確認開始時の水平断45・脳室系3/3・3D1面・配分40へ戻した。ブラウザ内配信SHA取得や全直交断/全画面QAではない。
狭幅見出しの省略など既存UI課題は残る。専門家レビューや全分節完成・公開反映ではない。以下は過去の工程履歴。

## 最新：側脳室の脳槽側80点を除外（統合検証完了）

現在の開発ラベルは `a512880c4dcd1b1291664f8ee4aaa3bd8d609b62dd2e037634953cd7ebd12efd`、
rawは `a972bd3e0e47d395112bfe29d7587aac0248e992a6720ce052f4c0b5da10c1d5`、ID24=63794。
80点の24→0のみで、断面の側脳室/全脳室meshと5ブロック部品を同期。第三/第四mesh不変。
原300全66面・native100代表点の根拠、55部品への影響、復元用7d2bと旧5mesh、採用記録は
LATERAL_VENTRICLE_FRINGE_REVIEW.mdを参照。特定の脳槽名へ再分類してはいない。
新旧採用テスト8/8と導入3/3成功。全Node初回は556/557で、乳頭体テストの旧監査参照1件を修正し対象1/1成功。
旧失敗logは保持し、`work/lateral-residual80-full-node-v2.log` で557/557成功（169883ms）。
型検査・本番build成功。右4345をreloadし、水平断69（修正付近）と透過3D、側脳室・脈絡叢・内側側頭葉の
3ブロックをそれぞれ開き描画を目視した。最後は水平断69・脳室系3/3・3D1面・表示配分40へ戻した。
ブラウザ内配信SHA取得や全直交断/全画面の再監査とは主張しない。見出しの狭幅省略等は残存UI課題。
全分節完成・専門家確認・公開反映ではない。以下の「最新」は過去の工程履歴。

## 最新：側脳室の溝状飛び地547点を開発版へ除外適用（統合検証完了）

`install_lateral_detached547.py --apply` の16ファイル事前検査後に採用。全3方向117原300面と全脳locatorの
判断根拠は `LATERAL_VENTRICLE_FRINGE_REVIEW.md`。修正は547点の24→0のみ。
現圧縮SHA `7d2b88c3e966b9633571e1d5cfe4d86a99439e2ea7873c672217abd4c235a1f2`、
raw `c82bde4c3ea2c79bdd29ff4a964bc48f7169f1cbac089f9d323fbe7078cd7fff`。
ID24=63874、他の非0ラベル不変。旧b45cと変更する3ブロックmeshをtests/fixturesへ保存。
採用記録 `segmentation-patches/review/lateral-detached547-adoption-2026-09-07.json`。
断面の側脳室/脳室系全体meshと側脳室block実質/腔・脈絡叢block腔を同期。第三/第四mesh不変。
全55mask影響、実質への追加0、変更前生成byte一致を確認。Python差分/mesh4/4・導入2/2、型検査成功。
最新SHAを参照する視索/乳頭体の数値監査も新規reportへ実再計算し、以前のreportを保持した。
全Node556/556（`work/lateral-detached547-full-node-v2.log`、175272ms）、本番build成功。
右4345をreloadし、水平断位置60/59の断面＋透過3D、側脳室blockと脈絡叢blockの描画を目視確認。
最後に水平断位置59・脳室系3/3選択へ戻した。初回UIへvoxel番号152を渡した操作は範囲外として拒否され、
表示位置0–100の59へ訂正（この拒否はデータ不具合ではない）。全面/全直交断QAやブラウザ内配信SHA取得とは主張しない。
画像境界の根拠は原300の117面と全脳locatorによる。main/公開変更なし。
以下のb45c「最新」は8点修正時点の履歴。

## 最新：第三脳室8点を開発データへ適用、統合検証完了

`install_third_detached8.py --apply` の全事前検査後に適用。
現ラベルSHA `b45c0669122b628529f56e73af06fa1cb697b621da99d51c8b921b136ea52463`、
raw `e23a6272e399a4ba696c7f60157120281a994f7cc9949a86aaa9dbfbb146bc4c`。
ID25=12015、他ラベル不変。旧ffb8をtests/fixturesのpre-third-detached8へ保存。
採用記録 `segmentation-patches/review/third-detached8-adoption-2026-09-07.json`。
最新SHA・視索/乳頭体の数値監査を更新し、旧105点テストは復帰fixtureと後続の8点記録へ連鎖させた。
型検査成功。全Nodeは `work/third-detached8-full-node-v1.log` で555/555成功（155924ms）。
Python導入2/2、差分/mesh3/3、画像被覆1/1も成功。本番build成功。
右4345をreloadし、第三脳室選択・水平断54への移動・断面と透過3D描画を目視確認。
今回のブラウザ確認はこの水平断/選択操作に限定し、3直交断の全面再検査やブラウザ内配信SHA取得は行っていない。
断面ごとの境界根拠は既述の原画像・差分図による。以下の未採用記述は工程履歴。
main/公開更新なし。

## 後続の統合検証と飛び地の全範囲棚卸し

第四脳室105点採用後のffb8e56e…を含め、全Node554/554・型検査・本番build成功。
ローカル直交断/3D確認と右プレビュー更新済み（FOURTH_VENTRICLE_REPAIR.md）。
以下のe98c・未採用・最終再実行待ちは同期導入時点の履歴。

`inventory_current_ventricle_components.py` で全体格子の各IDを独立に計数し、
全6近傍成分のXYZ範囲・実在seed・26近傍親成分を記録した。
出力 `work/anatomy-review/current-ventricle-components-ffb8-v1.json`、入力SHA ffb8e56e…。
ラベルやmeshを変更しない。辺/角での接触と面接触を区別する合成テスト2/2成功。

| ID | voxel | 6近傍成分 | 26近傍成分 |
| --- | ---: | ---: | ---: |
| 23 | 64838 | 65 | 25 |
| 24 | 64421 | 76 | 21 |
| 25 | 12007 | 32 | 18 |
| 26 | 8641 | 2 | 2 |
| 41 | 16 | 1 | 1 |

次の照合先を具体化した：ID24の1726点（XYZ245–275/197–258/85–133）は
26近傍では主成分と同じで、独立した誤分類として削除する根拠にならない。
一方ID23の660点は26近傍でも別677点成分、ID24の486点は別547点成分、
ID25の94点は別137点成分に属する。これらの分離は数値所見であって誤分類判定ではない。
ID26の上方2点[193,195–196,114]は依然として両方式で独立し、原画像による帰属確認が必要。
次は独立成分の原画像・連続断の再照合を行う。サイズ閾値で削除せず、直線で接続しない。

## 原因と範囲

### 第三脳室の独立137点の原300照合（2026-09-07）

8点候補の差分確認完了：`third-detached8-difference-v1` の全13PNG36面
（X192–196、Y242–260、Z163–174）を目視。7点は上端の明るい空隙側、
1点[195,259,164]は別の局所縁にあり、灰色組織を横切って本体へ接続しない。
広い空隙の残存や細い組織はこの8点では解決しない。
`stage_third_detached8.py` で固定8点のみ0→25をworkへ保存し、逆適用で入力全体との一致を確認。
ステージSHA `b45c0669122b628529f56e73af06fa1cb697b621da99d51c8b921b136ea52463`。
記録 `third-detached8-stage-v1/repair.json`。まだ教材には未採用。次は関連mesh影響と統合検証。

8点のmesh影響確認：全55ブロックmaskは不変。現行ブロック用の2格子間引きでは
8点が採用格子にないためであり、解剖学的に無意味な修正ということではない。
全範囲0.5mmのsection meshは修正前4資産を現在値と完全再現照合できた。
修正後は第三脳室と脳室系全体の2meshのみ変化し、側脳室・第四脳室はbyte不変。
第三脳室は12007→12015 voxel。`third-detached8-meshes-v1` と
`third-detached8-section-meshes-v1` にwork-only保存。次は導入前検査と現行SHA依存監査の同期。

導入前検査を追加：`install_third_detached8.py` は固定ステージSHA・全8点逆再生・
13差分画像・55部品IDの完全被覆・2倍間引き前後配列の完全一致・全範囲mesh再生成を検査する。
他作業のラベル/mesh変更や復帰記録の衝突を拒否する。9ファイルの更新計画は成功、
まだ `--apply` は未実行。読み取り専用計画と誤った再生処理の拒否テスト2/2成功。
次工程で現行SHAに依存する監査/テストを同時更新し、適用後の全体検証を行う。

追加native100照合：`render_fornix_native100_connection.py --third-detached` で
app[193,248,168]/[193,241,172]を逆変換し、全6PNG18面を目視した。
`third-detached137-native100-v1`。両参照点は明るい空隙側に見えるが、周囲に細い組織がある。
137点全域の境界確認ではなく、全小片削除・一括接続を支持しない。
往復変換最大誤差3.5058e-7 mmは数値精度であって解剖学的登録精度ではない。
既存の原画素再復号・パネル完全一致テストへ今回の6PNG/18面を追加した。

`diagnose_third_detached_support.py` は137点とその0ラベル面隣接274点を測定。
原300補間支持点の最小値65000以上は既存137中10点、隣接274中8点。
低支持値だけで既存点を削除しない（部分体積・組織混在を含み得る）。8点も未採用で、
次の差分目視対象に絞っただけ。`third-detached137-support-v1.json` に全座標/値/入力SHAを保存。
ラベル・mesh・公開は変更していない。

`review_third_ventricle_native300.py --detached137` のv2全26PNG・78面を目視完了。
X320–334、Y401–436、Z264–290の連続面。現ffb8ラベルを科学affineで最近傍投影し、
原300未着色画像と並記した。v1は端の連続面が不足したため保存のみとし、v2を採用する。
出力 `work/anatomy-review/third-ventricle-detached137-native-v2/report.json`。

所見：独立した上方の小label付近は一様な空腔ではなく、灰色組織と明るい隙間が混在する。
Y419–424/Z273–281では本体との間に灰色の筋状・小塊状部分があり、直線接続は不適切。
X323–331でも輪郭付近の組織形状が連続的に変わる。小片の周囲には未着色の明るい領域もあり、
137点を一括で誤分類として除去する証拠も得ていない。表示窓で明るいことだけを腔の確定根拠にしない。
次の判別は当該137点の原画素支持・native100による内部組織との境界で行う。
今回label/mesh変更なし。これは小領域の追加レビューで、第三脳室全体や組織名の確定ではない。

`structureMeshFiles.ventricle` は `build_atlas_volume.py` の旧CerebrA由来
`ventricle.mesh` を参照していた。同資産は側脳室のみではなくFOCUSの8ラベルを
まとめた領域で、現在のBigBrain断面ID23/24とは一致しない。
分節モードの背景 `segment-ventricles.mesh` も旧CerebrA由来だった。
第三・第四脳室の選択には局所ブロック部品が使われていた。

BigBrain断面選択だけに専用マッピングを追加し、全範囲のID23/24、25、26を
個別に再構成。BigBrainの分節背景には23/24/25/26/41の全範囲を使う。
MNI画像時・自由観察・ブロック標本の既存参照は変更しない。

## 再現・根拠

- 再生成: `scripts/build_section_ventricle_meshes.py`
- 入力/出力SHA・全6近傍成分サイズ: `public/atlas/section-current-ventricles.json`
- 入力SHA: `e98cd4060d735c732a5fd75030be2f701f57fe91b6cd5b9a12c65e1cb68b37e3`
- 0.5 mm全格子、marching cubes level 0.5、平滑化・間引き・穴埋め・成分削除なし。
- 表示格子XYZ原点[-98,-116,-90]、保存順ZYX。原画像照合用の科学座標原点とは別。
- 側脳室129259 voxel/141成分、第三12007/32、第四8536/2。
  成分数は6近傍による。斜め接触も分離として数えられ、すべてが誤分類とは限らない。
- ラベル本体に変更なし。飛び地を見た目だけで消さず、小片・途切れを可視化したまま残す。

## 確認と未完了

Python再構成テスト2/2、TypeScript型検査、本番ビルド成功。
Node追加テスト2/2成功、diff-check成功（既存CRLF警告のみ）。
全Node回帰v1は546/550。追加5資産のmanifest登録漏れと2つの旧コード文字列期待を修正。
v3は551/552で、残りは資産数110→115の固定期待だけだった。
期待数を正しい115へ更新後、権利監査・新meshテスト13/13成功。
ログ `work/section-ventricle-mesh-node-v3.log`。最終全体再実行は次の統合チェックポイントで行う。
通常buildも再生成済み。全552同時成功とはまだ記載しない。
右のローカル4345を再読み込みし、水平断50・脳室3項目・透過3Dの描画を目視確認。
全面監査・残存飛び地の帰属判定・黒い領域の追加分節は未完了。
第四脳室105 voxel候補も未採用。これは表示データ同期であり専門家確認ではない。
公開サイト・mainは未変更。
