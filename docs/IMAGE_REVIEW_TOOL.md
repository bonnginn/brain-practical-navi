# ローカル画像注記ツール

起動: `node scripts/serve_image_review.mjs`。表示された127.0.0.1のURLを開く。
教材本体のビルド・公開には含めない。新規依存なし。サーバーは使用中は維持する。

初期画像は `work/user-review-midbrain-limit.png`。ブラウザでPNG/JPEG/WebPを
選び直せる。マウス・ペン・指による自由線、線色・太さ、Undo、全消去、表示倍率、
補足、PNGダウンロードに対応。入力画像は16MB/1600万画素まで。
画像切替・ページ離脱時には未保存の変更を警告する。自動保存ではない。

「確認を保存」で `work/image-review-responses/<日時-UUID>/` に保存する。

- `source.png`: 注記を描く前の画像（PNG再符号化）。
- `annotated.png`: 描いた線を重ねた画像。
- `annotation.json`: 原画像ピクセル座標の線、画像寸法、元入力ファイルSHA、
  保存した両PNGのSHA、補足と日時。

レビュー結果を探す際は、このフォルダーの新しいannotation.jsonを確認する。
保存成功はannotation.jsonがあることを確認し、PNGだけの不完全保存を採用しない。
線を全3Dへ投影したり、保存だけで専門家承認にしたりしない。
元ラベル・元画像は上書きしない。sourceHashはユーザー入力ファイル、
sourcePngSha256は再符号化PNGのハッシュなので一致するとは限らない。

## セキュリティと公開範囲

127.0.0.1のみlisten。任意のファイルを公開する静的サーバーではない。
厳密なHost、同一Origin、セッションごとの確認トークン、JSON/body上限、
線座標・画像寸法・PNGヘッダーを検査する。ファイル名をユーザー入力から
生成せず、UUIDで新規保存する。外部送信や自動公開はない。
ローカルの信頼された利用者向けであり、インターネットへ公開しない。

## 2026-09-06 検証

- `tests/image-review-tool.test.mjs` 2/2: 不正座標・寸法・形式の拒否、
  cross-origin拒否、任意パス拒否、保存の一意性、記録の非承認状態。
- Node構文検査成功。
- Chrome152の実pointerイベントで1366/390幅の描画→保存→Undo→再保存を実行。
  `work/image-review-browser-test-v1/` に試験専用4保存を隔離。
  ユーザー回答フォルダーへは試験データを書かない。
- 両幅のスクリーンショットを実際に目視。横overflowなし。
  物理タッチ端末やすべての画像形式の実機試験ではない。
- この独立ツール追加後、教材全体の635件を再実行したとは主張しない。
