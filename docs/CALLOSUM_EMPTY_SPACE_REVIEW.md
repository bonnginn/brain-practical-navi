# 脳梁試作ラベルと原画像の空白領域 — 2026-09-06

## 後続採用との関係

以下は930e…を対象とした調査時点の記録。3間隙291 voxelは、後続の皮質はみ出し1,314 voxelと合わせた別のstrictプロジェクト採用記録により開発版へ反映した（[局所修復記録](CALLOSUM_LOCAL_REPAIR.md)）。この文書が参照する旧候補JSON自体はunreviewedの履歴として保持し、二重適用しない。再現CLIは旧930e…fixtureを読む。現行版は5348…であり、以下の「未採用・現行不変」は調査当時の状態を指す。

## 調査結果（採用前の履歴）

追補: 上位3成分の全占有X/Y/Zと各方向の前後1枚、計97局所断面を24シートへ出力し、すべてAIが目視した。3全体locatorはv1と同じ内容で全体位置を確認済み。`work/anatomy-review/callosum-empty-overlap-complete-v2/`。いずれも候補外縁が組織間の明るい間隙に及び、隣接断で持続することを確認した。3成分291 voxelだけの30→0差分を `segmentation-patches/review/callosum-three-gap-candidate-2026-09-06.json` に保存。strict／medium／unreviewed、**未採用**。97断面を見たことは脳梁全境界の精査完了ではない。

`audit_callosum_empty_overlap.py --complete --candidate` でworkへ再生成できる。入力SHA、3成分の数とbbox、全291座標を固定し、任意の上位3成分へ自動転用しない。`tests/callosum-gap-candidate.test.mjs` 1/1で入力SHA・正確な座標集合・原値30／画像値255・成分数79/81/131・未採用で現行資産不変を確認。patch v1の `primaryPlane=horizontal` は既存の水平断編集契約であり、根拠は三方向を含むと明記する。

現行開発volume `930eaaed7eed8782b1b162f3aa5c59c2428f4062d0d2da3a9a1cb563f49b7db7` のID30は151,380 voxel。そのうち原画像値255に重なる部分は1,208 voxel、6近傍286成分。ID31/32の同じ重なりは0。255は元データの高値／非有限値を含む表示用符号化であり、組織不存在の保証ではない。

`audit_callosum_empty_overlap.py` はvolumeを変更せず、全成分の数・bbox、上位3成分の各軸5連続断＋全体矢状断locatorを生成する。`work/anatomy-review/callosum-empty-overlap-v1/report.json` に入力・画像SHAを保存。全12画像、45局所断面と3全体図をAIが開いて目視した。

| 成分 | voxel | XYZ最小–最大 | 目視所見 |
| --- | ---: | --- | --- |
| 199 | 131 | [206,301,192]–[216,314,198] | 前上方の弧状ラベル外縁が明るい組織間隙へ及ぶ。隣接矢状・冠状・水平断で持続 |
| 213 | 81 | [207,287,205]–[216,292,210] | より上方の折れ曲がる表面溝状の間隙へ角形の候補が及ぶ |
| 227 | 79 | [207,318,183]–[216,326,188] | 前下方へ向かう弧の外縁が組織間の明るい間隙を横切る |

原画像と一致しない外縁があることは確認した。一方、この3成分を除くだけでは脳梁と周辺の帯状回／白質の境界全体を直したことにはならない。残り917 voxelを同じ理由で一括削除しない。まず候補全占有面と位置を追い、255が間隙なのか収録不良／部分体積なのかを分ける。既存の直線的な側壁制限も別問題として残る。

生成器は脳梁候補を白質確率・正中距離・脳室距離から作り、候補30–32を実画像のempty_spaceで再制限しない。これは重なりの生成上の説明だが、単純な閾値マスクを解剖学的な採用根拠としない。今回patch・開発ラベル・meshを変更していない。
