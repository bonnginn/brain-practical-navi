# 公開α 発表品質監査

判定基準: `未確認` / `要修正` / `発表可（α注記あり）`

| 画面 | UI・レイアウト | 操作導線 | 解剖・説明 | 判定・残課題 |
| --- | --- | --- | --- | --- |
| トップ | 情報階層・余白・4導線を確認 | 3D回転と各教材への導線を確認 | 表面モデルを「分節」と誤記していた点を修正 | 発表可（α注記あり） |
| 脳表 01 左外側面 | 全選択による色競合を廃止し無着色で開始 | 個別追加・全選択・解除を確認 | 中心前回の単独表示と外側面形状を確認 | 発表可（α注記あり） |
| 脳表 02 上面 | 無着色で開始、固定方位記号を撤去 | 中心前回と中心溝の併用を確認 | 中心溝を連続する専用ガイドへ変更 | 発表可（ガイドは模式補助） |
| 脳表 03 下面 | 無着色の標本から開始し情報過多を解消 | ページ移動時の解説欄スクロールを初期化、個別追加を確認 | 視神経・視交叉・視索の単独表示と下垂体非表示注記を確認 | 発表可（補助構造は模式表示） |
| 脳表 04 左半球内側面 | 小脳・脳幹・中脳を除いた左半球単独の初期表示へ整理 | 皮質・溝は無着色開始、深部は明示選択時だけ模式表示 | 脳梁―透明中隔―脳弓を左側成分だけで再確認し、透明中隔を間隙内の三角形へ縮小 | 発表可（深部は模式オーバーレイと明記） |
| 脳表 05 脳底の主要動脈 | 凡例・操作バーの重なりを解消 | 透過脳表を初期表示、構図を縮小 | 模式3Dであることを明記 | 発表可（形状精度は継続監修） |
| 脳表 06 脳神経・脳幹 | 透過脳表に脳幹を明瞭化し、未説明の視床下部補助形状を撤去 | I・Vの個別強調と直接URL時のI初期選択を確認 | 神経根は近位走行のみの模式3Dである注記と脳幹高位との関係を確認 | 発表可（遠位走行・核・孔は対象外） |
| 脳表 自由観察 | 空状態・検索結果・選択カードの階層と余白を確認 | 中心前回の検索追加、複数選択、解除、深部選択時の左半球自動切替を確認 | 深部構造は片側の模式表示、皮質は表面分節として区別 | 発表可（構造ソースの種別を常時表示） |
| 断面 冠状断 | 構造グループを可読な1列へ整理 | 4画像ソース、標本調・学習図・輪郭、構造表示を確認 | 単一標本・固定脳MRI・平均T1・T2の由来差を確認し、T1/T2コントラストを明確化 | 発表可（固定脳MRIは画像参照のみ） |
| 断面 水平断 | 中央断と下方断で画像・指標・全脳インセットを確認 | ランドマーク移動と切断面追従を確認 | 脳室体部、橋・第四脳室・小脳レベルの連続性を確認 | 発表可（試作ラベルは明示） |
| 断面 矢状断 | ページ切替時に正中付近へ戻し、極端な外側断開始を解消 | 脳梁追加、クリック同定、連続再生・停止、同定ラベル文字重なりを確認 | 正中構造と側脳室の試作分節を由来表示つきで確認 | 発表可（クリック同定はPILOT表記） |
| 標本 01 側脳室 | 部品カードを1列化して日英名称の切れを解消 | 全表示・側脳室単独・組織透過を確認 | C字形の腔と尾状核・視床・海馬の隣接を確認 | 発表可（側脳室は試作分節） |
| 標本 02 間脳 | 解説欄をページごとに先頭へ戻す | 5レイヤーと組織表示を確認 | 第三脳室を基準に視床・視床下部・視床下核・乳頭体を確認 | 発表可（位置目安を区別） |
| 標本 03 投射線維 | 縦長標本と7レイヤーの収まりを確認 | 部品個別切替と視点プリセットを確認 | レンズ核・内包・放線の模式走行を確認 | 発表可（放線は走行目安） |
| 標本 04 交連・脳弓 | 4構造の表示階層を確認 | 透明中隔の単独表示と全表示を再確認 | 巨大な楕円板だった透明中隔を脳梁―脳弓間の三角形へ縮小 | 発表可（透明中隔は模式補助） |
| 標本 05 脈絡叢 | 3レイヤーの簡潔な構成を確認 | 腔・脈絡叢・海馬の着脱を確認 | 脈絡裂に沿う付着位置を模式表示として確認 | 発表可（脈絡叢は模式補助） |
| 標本 06 内側側頭葉 | 小標本と5レイヤーの視認性を確認 | 海馬・扁桃体・下角・海馬采・鉤を確認 | 前後方向の局所関係を説明と照合 | 発表可（鉤は位置目安） |
| 標本 07 中脳横断 | 初期表示が小さすぎたためページ固有に拡大 | 9レイヤーと標本組織の重なりを確認 | 赤核・黒質・中脳水道・大脳脚・丘・膝状体の層序を確認 | 発表可（複数項目は模式補助） |
| 標本 08 脳幹・小脳 | 10レイヤーの縦リストとモデル内操作の重なりを確認 | 小脳・橋延髄の脱着、初期・上面プリセットを確認 | 第四脳室・小脳脚・菱形窩表面目安の露出を確認 | 発表可（核・隆起は位置目安） |
| 復習クイズ | 問題画像・選択肢・採点欄・空状態の階層を確認 | 正答・誤答・次問・間違い問題のみ・履歴消去・全問題復帰を確認 | 問題画像のラベル由来と解説文を確認 | 発表可（試作ラベルの由来を表示） |
| 編集ツール | 右見出しの孤立改行と狭い左案内を調整 | 81 voxel塗布、元に戻す、やり直し、再度元に戻して0 voxelへ清掃 | 元ラベル非破壊・端末内差分・PRレビュー前提の説明を確認 | 発表可（共同制作者向けα） |
| 意見・共同制作 | 2対象者別カード、背景マスク、閉じる導線を確認 | 匿名報告・共同参加のフォーム導線を確認（外部送信は未実行） | 個人情報・患者情報を送らない注意と公開α方針を確認 | 発表可 |
| 利用条件・クレジット | 2列のライセンス情報とスクロール、閉じる導線を確認 | 対応ソース・外部データリンクを確認 | 稲葉弘哲・三重大学大学院 医学系研究科 組織学・細胞生物学を控えめな1行で掲載、メール非掲載 | 発表可 |

## 第二巡（再起動後）

- 内側面クイズも通常の内側面教材と同じ左半球単独表示に統一し、小脳・橋延髄・中脳が再出現する不一致を解消。
- 760 px以下の上部ナビを2段化。390 px幅でもワークスペース名を潰さず、現在項目を横スクロール中央へ自動表示。
- 断面3方向と標本8種へ個別ハッシュURLを付与。再読み込み・履歴移動後も選択ページへ復帰。
- トップの3Dを分節表示から表面表示へ変更し、「全脳表面モデル」という説明と視覚表現を一致。
- BigBrain原著年を Amunts et al., 2013 に訂正し、固定脳MRIの格子表記を 0.444 mm に統一。
- 共有WebGL描画器でフレームごとのシェーダー資源を解放し、長時間の回転・連続巡回での資源蓄積を抑制。
- WebGL描画で未使用だった約6 MBの `brain.mesh` を全3D画面の起動時に取得する処理を除去。再現用ファイルは同梱したまま、初回転送だけを削減。
- 通常幅の上部ナビを圧縮し、1198 px幅で教材タブと右上アクションが重なる問題を解消。
- 全脳表ページに「向きを戻す」を追加。下面は小脳を初期非表示にし、腹側の観察対象が小脳に隠れる構図を改善（必要時は表示可能）。
- 390 px幅で脳底動脈・脳神経の凡例が縦方向へ過伸長する問題を修正。トップ、断面、標本、クイズ、編集ツール、両ダイアログでも横はみ出しなしを確認。
- 「公式α版」と読める利用条件の表現を「公開α版候補」に統一し、公開判断前の状態を誠実に表示。
- 意見募集と利用条件に `#workspace/feedback` / `#workspace/legal` を付与。直接共有・再読み込みが可能で、閉じると元の教材画面へ戻る。
- 通常幅でトップ、脳表7種、断面3方向、標本8種、クイズ、編集ツール、意見募集、利用条件の全23経路を連続巡回。URL・選択状態一致、描画待ち残り0、横はみ出し0、Viteエラー表示0。
- 意見フォームの回答者URLへWindowsブラウザから到達し、ログイン前にも質問が表示されることを確認。ライブフォームだけ旧「β版」表記だったため、同梱Apps Scriptによるα表記への同期を公開前の人手作業として記録。
- アプリ内「対応ソース」先のGitHubリポジトリがログアウト状態でもPublicとして閲覧でき、PR #1が引き続きDraft、対象ブランチが `codex/alpha-ui-quiz-fixes` であることを確認。
- ダイアログは背景スクロールを止め、閉じるボタンへ初期フォーカスし、Tabを内部循環させ、閉じた後に起点へ戻す。断面実習の主見出しと差分JSONファイル入力の読み上げ名も追加。
- 第二巡の自動検査は型検査成功、32/32テスト成功、本番Viteビルド成功。
- 透明中隔は生成式のみが先行していたことを検出し、配布メッシュも再生成した。単独レイヤー表示で脳梁と脳弓の間に収まることをWindows画面で確認し、形状範囲の回帰テストを追加した。
- 320 / 390 pxの全23経路を追加巡回。狭幅で固定下部ナビに押し込まれていたクイズ設定・編集説明を本文フローへ変更し、標本名の縦切れと断面スライダー目安名の重なりを解消した。クイズは5問設定、開始、回答、次問題表示まで実操作した。
- 最新の本番ビルドを通常幅・320 px幅で各23経路連続巡回し、全URL一致、描画待ちなし、コンソール警告・エラーなし。脳神経ページの共有URLは `surface/nerves` に統一した。
- 局所標本は不要な高密度脳表一式を先読みしないよう変更し、直接アクセス時は選択標本の部品だけで描画できることを確認した。
- 1366 × 768 pxでも全23経路を巡回し、トップ、水平断、クイズ、編集ツール、クレジットを重点目視。描画領域、操作欄、控えめなProject Lead表記に切れ・重なりなし。
- 公開用のテーマ色、SVG favicon、Open Graph、Xカード用メタデータを追加し、本番成果物への収録を回帰テストで固定した。
- 320 px幅で脳表カードの「向きを戻す」が右端で約19 px欠けるケースを最終巡回で検出。見出し操作群を狭幅だけ折り返し、全脳表ページで操作ボタンが可視領域内に収まることを再確認した。
- 代表8画面の可視テキストを実計算色で走査し、補助矢印、公開αラベル、全選択・選択だけ、クイズ選択肢記号、編集説明のコントラストを4.5:1以上へ調整した。
- 汎用ブロック図形だったfaviconを、画面配色と一致する左右半球・正中切断線の脳モチーフへ変更。OG画像、favicon、全ローカル文書リンクが本番成果物へ収録されることを確認した。
- 標本を素早く連続切替した際、古い非同期読み込みが新しい標本へ混入しない破棄ガードを追加。側脳室→間脳→中脳→後脳を連続操作し、最終URL・見出し・選択表示・描画が後脳で一致した。
- 冠状・水平・矢状断の各方向で、単一標本0.5、固定脳MRI 0.444、平均T1、T2の全12組合せと、実習標本調・学習図・輪郭の全9組合せを実操作。選択状態、描画完了、URL保持に不一致なし。
- 標本8画面・全57レイヤーを個別に非表示→再表示し、各画面で組織の透過／通常／非表示と初期／反対側／上面／下面を実操作。最終状態、URL、見出し、描画完了に不一致なし。
- 脳表7画面で向きリセット、領域・溝の全選択／全解除を実操作。各URL・見出し・Canvas描画を維持し、最終的に補助着色0の観察開始状態へ戻ることを確認した。
- クイズを10問完走した際に全問の正答がAになる設計欠陥を検出。問題順だけでなく4選択肢も出題・再挑戦ごとにシャッフルするよう修正し、10問の正答位置がA/B/C/Dすべてへ分散することを実操作で確認した。

## 正確性・3D閲覧性の再調査

- BigBrain、Allen Human Brain Atlas、NCBI Bookshelfの解剖資料、Willis動脈輪の原著研究を照合。BigBrainの0.5 mm表示は原取得解像度ではなく20 µm再構成からの再標本化であること、動脈輪の模式は典型形で個体差が多いことを画面上に明記した。
- I・IIをIII–XIIの脳幹神経根と区別し、IVの背側起始後の経路を現行3Dでは簡略化していると注記した。
- 左内側面は皮質だけから開始し、脳梁・透明中隔・脳弓・視床・視床下部は左成分を選択時だけ重ねる。透明中隔は脳梁下面と脳弓上面を結ぶ両葉性隔壁の左葉模式と説明した。
- Neuroglancer、BrainBrowser、NiiVueの操作設計を参照し、動的R/L・A/P・S/I方位、画面上の±ズーム、矢印キー回転、Rキー初期化、キーボードフォーカス表示を追加した。
- Windowsの右側ブラウザ（約624 px幅）でホーム、断面3方向、脳表7種、標本8種、クイズ、編集ツールの20経路を再巡回。全経路で横はみ出しなし、方位・ズームが3D領域内、コンソールエラーなし。奥行き軸の方位文字が重なるケースは表示を抑制し、常設凡例で補った。
- 検証結果は型検査成功、34/34テスト成功、本番Viteビルド成功。

## M1 Windows実ブラウザ回帰（β候補ブランチ）

- 環境: Windows Chromium 151、`http://localhost:4173/` の本番Viteビルド。通常幅1366 × 768 px、境界幅760 / 761 px、狭幅390 × 768 px。
- トップ、脳表7種、断面3方向、標本8種、クイズ、編集ツール、意見募集、利用条件の全23経路を、各幅で直接URLと再読み込みの両方から確認。URL不一致、横はみ出し、読込残り、画面内エラーはいずれも0。
- 上部の6教材ナビを実クリックし、トップ、脳表、断面、標本、クイズ、編集へ正しいハッシュURLで遷移することを確認。
- 脳表でドラッグ回転、ホイール拡大、向きリセット、複数選択、全解除を実操作。断面では構造グループの複数選択、断面拡大、切断位置3Dの表示を確認。
- クイズで正答後、「観察画面で復習」から水平断80・小脳選択済みの観察画面へ戻ることを確認。編集ツールではCanvasを1回塗って81 voxelの差分を作り、Undoで0 voxelへ戻した。
- 標本8種は案内画面だけでなく「試作品を確認する」後の全モデルを開き、各Canvas 1、読込エラー0、横はみ出し0。後脳の橋・延髄脱着時に錐体・オリーブの遊離表示がないことを描画と条件分岐の双方で確認。
- 水平断の2個の全脳インセットで同一アセット失敗を故意に発生させると、旧挙動では再読込ボタンを2回押す必要があった。`c0a3482` でエラー中Canvasの再試行を同期し、1回の操作でCanvas 3、読込残り0、エラー0へ復帰することを実ブラウザで確認。
- 低速回線模擬で読込進捗バーを目視・DOM確認。代表経路の初回アセット上限も自動検査へ固定し、42/42自動テスト、型検査、本番Viteビルドに合格。
- 共通の「操作ガイド」を追加し、3D、断面、着色・脱着、クイズ復習、編集ツールのポインター・キーボード操作を1画面へ集約。`#workspace/help` の直接表示・再読み込み、元の冠状断への復帰、Esc終了、Tab / Shift+Tabのフォーカス循環を確認した。
- 操作ガイドを390 × 768、760 × 800、761 × 800 pxで確認。横はみ出し0、390 / 760 pxでは1列、761 pxでは2列へ切り替わり、上部3ボタンも画面内に収まる。コンソールのerror / warnは0。43/43自動テスト、型検査、本番Viteビルドに合格。
- 水平断で大脳基底核を一括選択すると、`section-accumbens.mesh` の旧形式ヘッダーが三角形数ではなく頂点インデックス数を保持していたため、透過3D 2方向が読込失敗することを発見。実データ長と一致する旧形式だけを安全に正規化し、それ以外の不整合は拒否する互換読込へ修正した。
- 修正後、断面上で20構造を同時着色し、透過3D 2方向でも同じ選択を表示。Canvas 3、読込エラー0。「断面のみ」はCanvas 1、「3Dのみ」はCanvas 2へ切り替わることを確認した。
- レンズ核・投射線維標本で、組織透過、7層から選択中1層だけの表示、組織非表示を実操作。脳幹・小脳標本では小脳と橋・延髄を外し、両ボタンが「戻す」へ変わることと読込エラー0を確認。44/44自動テスト、型検査、本番Viteビルドに合格。
- 断面クイズのレンジ操作へ「1断面戻る／進む」を追加し、ドラッグが難しい環境でも切片を送れるようにした。水平断70から進むと表示値・見出し・Canvasが71へ同期し、出題位置へ戻すボタンが有効になることを実ブラウザで確認。
- 390 × 768 pxでも送り／戻しの30 × 32 pxボタン、レンジ、現在値、復帰ボタンが横はみ出し0で収まる。全断面問題の対象着色面積テストを含む45/45自動テスト、型検査、本番Viteビルドに合格。
- 隣接する内包と尾状核がほぼ同じ橙色（RGB距離6.2）だったため、内包を白質として淡色へ変更し、断面とレンズ核標本で統一。尾状核・被殻・淡蒼球外節・内節・視床とのRGB距離60以上を回帰検査へ追加した。冠状断で3構造を同時表示し、断面・透過3D 2方向とも色分離、Canvas 3、読込エラー0を確認。47/47自動テスト、型検査、本番Viteビルドに合格。
- 権利・プライバシー再監査で、Cloudflare Web AnalyticsのビーコンがローカルHTMLにも直書きされていた不整合を修正。公開HTTPS本番ホストだけで動的に読み込み、localhost、127.0.0.1、開発ビルドでは読み込まない。ローカル本番プレビューで解析script 0、利用条件ダイアログ、対応ソース、警告0を確認。
- `public/atlas/DATA-MANIFEST.json` を追加し、全配布ファイルが出典、改変、ライセンス、表示義務、同梱通知の1群だけへ属することを自動検査。標本全meshの `sourceType` と、脳表・脳底・神経血管の全meshが個別メタデータへ収録されることも照合した。
- 公開物のラスター／文書を監査し、画像はSNS共有用 `og.png` 1点のみ。講義・教科書・ウェブ標本・患者画像の転載ではなく、学習用正解図でもないことを `public/ASSET-NOTICE.txt` へ明記。50/50自動テスト、型検査、本番Viteビルドに合格。
- 公開Google FormをWindowsの未ログイン状態で確認。ログインは回答保存の任意導線で、匿名修正経路から3/3ページの送信ボタンまで到達し、返信先も任意だった。外部データを作らない監査のため送信は行わず、管理者による回答作成・削除の一往復は残課題として分離した。
- アプリの意見募集を、非公開・匿名のGoogle Form、公開相談・追跡のGitHub Issues、具体的変更のPull Request／共同制作ガイドの3入口へ分離。解剖監修、セグメンテーション、3D造形、Web実装の募集範囲も入口で明示した。
- 意見募集ダイアログの直リンク表示時、背後のレスポンシブ3D Canvasが一時的に0 pxとなり `drawImage` 例外を出す経路を実ブラウザログで検出。0幅・0高では描画せず、ResizeObserverによる次の有効サイズ更新を待つよう修正した。
- 修正後の本番ビルドを新規タブの1280 × 720 pxで再確認。3入口、横方向のはみ出しなし、最新アセット読込、console error/warning 0を確認し、52/52自動テスト、型検査、本番Viteビルドに合格。
