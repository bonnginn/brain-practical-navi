# クイズ出題粒度監査

更新日: 2026-08-23

## 目的と範囲

復習クイズの既存23問は、問題本文・正答target・選択肢・既存のトピック分類を変更せず、教材形式と表示方向で絞り込める。今回、由来区分だけを正答target単独から全選択肢を含む保守的な判定へ改めた。出題を増やす変更ではない。

形式・詳細の単一情報源は [`src/quizGranularity.mjs`](src/quizGranularity.mjs) の `QUIZ_GRANULARITY_BY_TARGET` で、アプリと監査スクリプトが同じ表を参照する。`origin` はtargetと全選択肢の由来から導く期待値を台帳へ記録したもので、全target・選択肢が解決済みかつ標準由来のときだけ標準とし、provisionalまたはunknownの選択肢を1つでも含む問題は試作とする。監査ではこのoption-aware判定、質問targetとの相互完全一致、重複、形式と詳細の不整合、問題行に重複して書かれた分類値と台帳の不一致を検査する。問題行に分類値を追加する場合も、台帳との一致が必要である。

## 現在の分類

| 分類軸 | 値 | 問題数 |
| --- | --- | ---: |
| 教材形式 | 断面・深部 | 17 |
| 教材形式 | 脳表 | 6 |
| 断面詳細 | 冠状断 | 9 |
| 断面詳細 | 水平断 | 7 |
| 断面詳細 | 矢状断 | 1 |
| 脳表詳細 | 外側面 | 2 |
| 脳表詳細 | 上面 | 1 |
| 脳表詳細 | 下面 | 1 |
| 脳表詳細 | 内側面 | 2 |
| 由来 | 標準 | 7 |
| 由来 | 試作 | 16 |

「標準／試作」は新しい問題内容を追加する判定ではない。既存の `isProvisionalQuiz` とtarget・全選択肢の構造由来を監査し、表の `origin` と一致することを要求している。試作内包を含む `pallidum`、旧ID33の `opticChiasm` を含む `accumbens` と `mammillaryBody`、試作島皮質を含む `hippocampus` は試作へ分類され、「試作問題を含む」をOFFにすると4問を候補から除外する。由来を解決できないtarget・選択肢は試作へ黙って分類せず監査を失敗させる。専門家未確認の問題は、従来どおり「試作問題を含む」で扱う。

旧ID 33 の混合視交叉・視索領域は正答targetへ戻していない。視交叉・左右視索の分節問題、脳神経・主要血管の同定問題はこの変更では追加していない。

「下面」は、既存の脳表問題のうち紡錘状回を対象とする位置関係の教材形式である。脳底の血管・脳神経同定問題を追加したり、下面という名前からそれらが出題されると示したりしていない。

## UIと候補数の契約

- トピック（従来の「出題項目」）、教材形式（すべて／断面・深部／脳表）、詳細（断面は冠状・水平・矢状、脳表は外側・上面・下面・内側）は独立して組み合わせる。
- 各selectの選択肢には、他の現在条件を適用した候補数を表示する。
- 「間違った問題のみ」と「試作問題を含む」は従来どおり候補抽出に含め、既存の端末内誤答履歴、試作既定ON、問題カードの試作バッジ、誤答後の観察画面リンクを維持する。
- 問題数ボタンは5／10／15／20問を上限として保持する。候補が上限未満なら「上限」と実際に出題される問数を併記し、候補数に応じてqueueをsliceする。候補が0件なら候補欄に0件を表示し、次回Startを無効化する。
- フィルタは「次回出題の条件」として扱う。条件を変えて候補が0件になっても、進行中の問題queueは隠さず、次回Startだけを無効化する。問題queueが空のときだけ空状態を表示する。
- 0件または空queueの説明は、設定欄内のstatus noteで特定の制約を原因と断定せず、候補数を確認してトピック・形式・詳細・試作問題・誤答履歴を見直すよう案内する。これにより、複数条件が同時に候補を0件にしている場合も誤案内しない。

## 監査と検証

監査スクリプト:

```text
node scripts/audit_quiz_granularity.mjs
```

期待結果は `ok: true`、23問、target重複0、断面17問、脳表6問、標準7問、試作16問である。異常系テストでは、形式と詳細の不一致、重複target、トピック／形式の組み合わせ、target・全選択肢のprovisional由来、unknown由来の拒否、由来除外、誤答履歴絞り込みを検査する。

2026-08-22 のローカル検証:

- 粒度監査テスト: 7/7 成功
- 問題本文・正答候補・position/viewの正規化スナップショットSHA-256: `7ee201594fd565acc152698e11c359e0d5fad3772ffa0b1d8d76c1e400b375fb`
- 既存を含む自動テスト: 125/125 成功
- TypeScript型検査: 成功
- 通常／Pages本番ビルド: 成功
- 実ブラウザ: Windows Chrome 151 のローカル本番ビルドで、初期23候補、脳表・内側面2候補、形式変更時の不適合詳細解除後の17候補、試作OFF時の0件status note、進行中queueの維持、2問出題、試作バッジ、誤答後の楔部を選択した内側面観察への復帰を実操作した。アプリ内ブラウザの390px相当表示でもselectと問題数表示の折返しを目視確認し、console error/warningは0件だった。
- 全経路監査: 同じ最終ビルドをChrome 151、1366×768／1024×768／390×768、26経路×direct/reloadで巡回し156/156件合格。missing/duplicate/fail、console/request/UI error、残留loader、横はみ出し、WebGL fallbackはいずれも0件だった。結果は `work/browser-audit/beta-route-audit-quiz-granularity-final-2026-08-22.json`（ローカル作業用・配布対象外）へ保存した。

2026-08-23 の全選択肢由来対応後のローカル検証:

- 問題本文・正答target・選択肢・position/viewのSHA-256は `7ee201594fd565acc152698e11c359e0d5fad3772ffa0b1d8d76c1e400b375fb` のままで、出題内容は変更していない。
- クイズ粒度／Go / No-Go／脳神経血管／描画HTMLの対象テスト101/101、全自動テスト206/206、TypeScript型検査、通常／Pages本番ビルドに成功した。
- Chrome 151のローカルproduction preview `http://127.0.0.1:4214/` で、試作ON時は全40問（標準7・試作33）、OFF時は7問（標準7・試作0）となり、新しく開始した7問queueに試作バッジがないことを確認した。辺縁系3問では標準1・試作2となり、暫定的な視交叉候補を選択肢に含む乳頭体問題へ「試作・専門家未確認」が表示された。
- 同じ最終ビルドの全経路監査は、Chrome 151、1366×768／1024×768／390×768、26経路×direct/reloadの156/156件に合格し、`allPassed: true` だった。結果は `work/browser-audit/beta-route-audit-option-provenance-2026-08-23.json`（ローカル作業用・配布対象外）へ保存した。
- 公開URL、物理端末、別GPU／別ブラウザ、問題内容と標準採用基準の専門家確認は未実施である。

この監査は問題内容の解剖学的妥当性を新たに保証するものではない。由来・確度・専門家レビューの状態は [`STRUCTURE_PROVENANCE.md`](STRUCTURE_PROVENANCE.md) と既存のクイズ監査に従う。
