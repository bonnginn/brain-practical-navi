# PWA・オフライン教材監査

更新日: 2026-08-15

## 方針

スマートフォン版はApp Store等へ配布する別アプリにせず、同じWebアプリをPWAとして提供します。初回訪問時に約71 MiBの教材全体を強制取得せず、アプリ本体は自動保存、閲覧した教材は逐次保存、まとまった教材は利用者が選んで保存する三段構成です。

教材セットは実ファイルからビルド時に生成し、容量とURLのずれを `npm run audit:pwa` で検査します。

| セット | 実測容量 | ファイル数 | 主な内容 |
| --- | ---: | ---: | --- |
| 脳表・3D観察 | 18.6 MiB | 34 | 左右半球、脳溝・領域、深部、脳底血管・脳神経 |
| 連続断面・構造同定 | 39.3 MiB | 21 | BigBrain、固定脳MRI、分節、断面用3D |
| 局所ブロック標本 | 18.8 MiB | 56 | 8標本と着脱部品 |

セット間で同じファイルを参照してもCache Storage上では同一URLを重複保存しません。クイズは脳表・断面セットを再利用します。

## 実装

- `manifest.webmanifest` は相対 `start_url` / `scope`、standalone表示、テーマ色、maskable対応SVGアイコンを定義。
- Service Workerは本番ビルドだけで登録。ハッシュ付きJS/CSSと軽量なアプリシェルを自動保存し、`atlas/` はオンライン優先・失敗時キャッシュへフォールバック。
- `#workspace/offline` でセットの容量、保存状態、進捗、ブラウザ割当容量を表示し、保存・更新・削除を利用者が明示操作。
- 教材セットごとに全ファイルの内容ハッシュから独立した版と保存完了状態を記録し、旧版は「更新が必要」、中断した保存は「一部保存済み」と区別する。脳表だけの変更で断面・局所標本を再取得させず、更新完了前に現行版とは表示しない。
- セット間の共有ファイルを記録し、一方を削除しても保存済みの別セットが必要とするファイルは保持する。削除したセットは共有ファイルが残っていても「未保存」と表示する。
- オンライン／オフライン、Storage Persistence、ブラウザ表示／インストール済みを表示する。対応ブラウザでは `beforeinstallprompt` を画面内の「インストール」へ接続し、それ以外はブラウザメニュー／iOS共有メニューを案内する。
- 旧アプリシェル／閲覧キャッシュは新Service Workerのactivate時に削除。明示保存した教材セットは保持し、オンライン時の再取得または「更新」で差し替え。
- 教材カタログはネットワーク優先・オフライン時キャッシュとし、新Service Workerが制御を引き継いだ時点で再取得する。新旧カタログが競合して更新済みセットを旧版と判定し続けない。
- GitHub Pagesのサブディレクトリ配信でも動くよう、登録・Manifest・教材URLは `document.baseURI` / Service Worker scope基準で解決。

## 2026-08-15 Windows Chromium実ブラウザ確認

本番Vite previewを `http://127.0.0.1:4174/` で開き、以下を確認しました。

- Service Worker controllerが有効。
- app shell、runtime、明示教材の3キャッシュが作成。
- 「局所ブロック標本」を保存し、56ファイル・18.8 MiBが「端末に保存済み」へ遷移。
- DevTools Protocolでネットワークを遮断したまま `/?m2=compare#workspace/blocks/hindbrain` を新規読込。
- アプリシェル、後脳比較画面、2つの3D Canvas、保存済み標本モデルが復元され、読込エラーなし。

同じ本番previewで保存版管理も回帰しました。

- 旧方式で保存済みだった局所標本を「更新が必要」と判定し、更新後にだけ「端末に保存済み」へ昇格。
- 脳表、断面、局所標本の3セットを現行カタログ版として保存し、3件とも保存済みになることを確認。
- 脳表セットを削除すると脳表だけが「未保存」になり、10ファイルを共有する断面セットと、独立した局所標本セットは「端末に保存済み」を維持。削除試験後は脳表も再保存した。
- 画面上で接続状態、Storage Persistenceが未許可であること、ブラウザ表示中であること、使用量／割当容量を確認。内蔵Chromiumではインストールプロンプトが発火しなかったため、実インストール操作は実スマートフォン待ちとし、メニューからの追加案内が表示されることだけを確認。
- 高密度脳表のgzip移行で実際にカタログを更新し、旧Service Workerが旧カタログを返す競合を再現。教材カタログのネットワーク優先化と `controllerchange` 後の再取得を追加すると、脳表18.6 MiBだけを現行版へ更新して「端末に保存済み」へ昇格できた。断面・局所標本は別の版番号を維持し、移行試験後は3セットすべて個別版の保存済み状態へ更新した。

## 継続確認

- iOS Safari / Android Chrome実機でホーム画面追加、OSによるキャッシュ退避、空き容量不足、バックグラウンド復帰を確認する。
- PWAは更新・外部フォーム・GitHubまでオフライン化しません。初回のオンライン読込と教材保存が必要です。
- ブラウザまたはOSは容量確保のためキャッシュを削除できます。実習前に保存状態を再確認する案内を維持します。
# 2026-08-15 保存前の容量保護

- `offline-packs.json` に各資源の実バイト数を追加し、合計値・URL順・実ファイルとの一致を `npm run audit:pwa` で検査する。
- 新規・中断保存では、明示キャッシュに未保存の資源だけを合計する。別セットと共有済みの資源は必要量へ重複加算しない。
- 旧版更新では全セット分を追加要求せず、置換中に必要となり得る最大1資源分を見積もる。
- Storage Estimateの推定空き容量が未保存分＋10%（最低5 MiB）の安全余裕を下回る場合、取得を開始せず、必要量・安全余裕・空き容量を表示する。
- 推定値は端末依存なので、利用者が危険を了承した場合だけ明示続行できる。`QuotaExceededError` は一般通信エラーと分け、不要セットまたはサイトデータの削除を案内する。
- Windows Chromiumの通常容量では3セットが保存済みとして再認識され、容量警告なし、画面内・console error 0を確認した。実際の容量不足・OSによる退避・明示続行後の挙動はiOS / Android実機待ちであり、この保護だけでPWA実機項目を完了扱いにしない。

## 2026-08-15 保存応答の健全性

Vite previewでは存在しないatlas URLもHTTP 200のSPA HTMLを返すことを実ヘッダーで確認しました。従来の `response.ok` とCache Storage上のURL存在だけでは、このフォールバックを正常教材として完了記録できるため、次を保存時・再確認時・未保存量算出時の共通条件にしました。

- HTTP成功であること。
- `Content-Type` が `text/html` ではないこと。
- `Content-Length` が提供される場合、カタログの実ファイルサイズと一致すること。CDN等が長さを公開しない場合は本文を全展開せず、この検査だけを省略する。
- 異常応答は完了数へ含めず、再保存対象として必要量へ戻す。

正常な高密度脳表は `Content-Type: model/mesh`、`Content-Length: 4996611`、正常JSONは `application/json` と実サイズを返し、欠損URLは `text/html` でした。HTMLフォールバック、サイズ不一致、HTTP 404の拒否を自動テストへ固定しました。本番ビルドのWindows Chromiumでは既存3セットが正常に保存済み、部分保存0、console error 0として再認識されました。本文SHA-256の再計算は大容量配列を端末メモリへ二重展開するため行いません。
