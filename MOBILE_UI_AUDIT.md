# スマートフォンUI監査

## 方針

スマートフォン版はPC版を単純に縮小しません。同じWebアプリと機能を維持しながら、760 px以下では次の専用レイアウトへ切り替えます。

- 上部: アプリ識別と操作・共同制作・利用条件だけを置く
- 下部: 6ワークスペースを常に移動できる主ナビゲーション
- 本文: 3Dモデルまたは断面を最優先し、操作、解説を縦方向に配置
- 画面別設定: 「断面・構造」「観察方向」「標本を選ぶ」「出題設定」「編集手順」から開くボトムシート

2026-08-15の初回案では教材選択帯とクイズ設定を通常フローへ置きましたが、PC用レールの縮小版という性格が残り、画面ごとに本文開始位置も揃いませんでした。再検討後は、主ナビを親指で届く下部ドック、教材内の設定を必要時だけ開くボトムシートへ分離しました。シートは元の全機能を保持し、閉じれば観察面を隠しません。局所標本の視点・着脱操作は引き続きモデル内へ残します。

ブラウザ間の文字自動拡大差を抑えるため、狭幅では `text-size-adjust: 100%` を明示します。文字そのものを画面幅に比例して縮小する設計にはしていません。

画面上端・下端には `safe-area-inset-top` / `safe-area-inset-bottom` を加え、ノッチやホームインジケータと固定UIが重ならないようにします。画面別設定シートは `dialog` として通知し、開いたら閉じるボタンへフォーカスを移動、Tabをシート内へ閉じ込め、Escまたは閉じる操作後は起点ボタンへ戻します。設定中は背景本文のスクロールを止め、シート内のスクロールだけを許可します。

## 2026-08-15 実ブラウザ回帰

- Windows Chromium、本番Viteビルド、390 × 768 px相当で確認。
- 脳表、水平断、内側側頭葉標本、クイズ、編集ツールの5系統を巡回。
- 全経路で文書幅375 px、横方向のはみ出し0、画面内エラー0。
- Canvas数は順に1 / 1 / 1 / 1 / 3。
- 初回案の通常フロー教材帯を再評価し、現在は6ワークスペース下部ドックと画面別設定シートへ置換。
- 局所標本はスクロール後も視点・着脱操作がモデル内に残り、下部ドックとの重なりなし。
- 390 / 760 pxでは専用レイアウト、761 / 1024 pxではPCレイアウトへ復帰し、全幅で横方向のはみ出し0。
- 320 / 390 / 760 pxで後脳標本の小脳・橋／延髄着脱、4視点、中脳標本の4視点と9構造表示を再確認。ボタンの機能とアクセシブル名を維持した。
- 320 pxではPC用の長い上部ユーティリティ名が折り返していたため、専用表示名を「操作・共同・条件」に短縮。操作ガイド、共同制作、利用条件という完全なアクセシブル名は維持した。
- 320 pxの矢状断で詳細解説を開き、長いラベル由来説明が横へ欠けず、縦スクロールで読めることを確認。
- 390 pxの断面実習で、PC側レールとともに構造追加操作まで非表示になっていた問題を修正。本文先頭の折りたたみパネルから6群・全22構造・全解除を操作し、側脳室・尾状核に内包・第三脳室を追加して4構造表示へ戻れることを確認。
- 391 × 639 px相当で、上部を54 pxの補助操作、下部を66 pxの6ワークスペースドックへ分離。文書横幅のはみ出し0。
- 断面画面のボトムシートを開き、3断面、6構造群、全22構造、全解除が縦スクロールで操作可能なことを確認。背景は設定中だけ遮蔽し、閉じるボタンと背景タップの両方を用意した。
- 同じ狭幅のまま脳表、ブロック標本、復習クイズへ移動し、「観察方向」「標本を選ぶ」「出題設定」の各シートと主要操作が残ることを確認。
- 1364 × 682 px相当へ戻し、PCでは左レール190 px、上部教材ナビ、スマホ用設定ボタン非表示へ復帰することを確認。
- M2比較経路は、PCの2 Canvas同時比較を維持しつつ391 pxではA/B切替式のCanvas 1枚へ変更。両モードの読込エラー0、切替状態、横はみ出し0を確認。
- 391 pxの画面別設定シートを `dialog` として開き、背景スクロール停止、Shift+Tabの末尾循環、Esc後の起点ボタン復帰を実操作した。Windowsでは安全域が0のため、実iOS端末でのノッチ・ホームインジケータ確認は継続。
- 型検査、本番ビルド、自動テストの合格を維持。小画面ヘッダー、着脱操作、断面構造選択の回帰条件を自動検査へ追加した。

## 未完了

実スマートフォンのタッチ入力、OS文字サイズ設定、GPU、メモリ、回線速度は未検証です。ネイティブアプリ化を先に行うとWeb版との二重保守が発生するため、まずこの専用Web UIを実機検証し、ホーム画面追加・オフライン利用・大容量アセット管理に明確な必要性が出た段階でPWA、さらに端末APIが必要な場合にネイティブアプリを判断します。
