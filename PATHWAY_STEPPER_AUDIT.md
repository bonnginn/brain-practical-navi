# 回路観察 3D＋断面ステッパー監査

更新日: 2026-08-24

## 目的と範囲

自由観察の「大脳基底核回路」は、回路全体を再現するものではなく、既存の手動分節ラベル・既存メッシュの位置関係を5段階で確認する小試作です。各段階は、既存クイズで使っている断面位置を再利用し、同じ構造を3Dと断面で同色に強調します。

この試作は新しい分節境界、模式線、結合、興奮性／抑制性、投射方向を追加しません。旧ID 33（視交叉・視索を混合した試作領域）は対象に含めず、クイズ正答や新しいground truthも作りません。手動選択はステッパーの段階選択とは別に保持します。

## 既存位置と画素監査

`src/pathwayStepper.mjs` の `BASAL_GANGLIA_STEPS` と `scripts/audit_pathway_stepper.mjs` で、`public/atlas/bigbrain-practical-segmentation-icbm500.bin.gz` のBBS1ラベルを、Canvasと同じ断面位置変換で走査しました。監査時には `app/page.tsx` の7構造の `bigbrainIds` も読み取り、監査用IDとの一致を検査します。画素数は対象IDの左右ラベルを合算した値です。これはその断面で表示可能なことの監査であり、形状の解剖学的正確性や専門家レビューを意味しません。

| 段階 | 既存位置 | 対象 | ラベルID | 画素数 |
| --- | --- | --- | --- | ---: |
| 1 | 冠状断 61 | 尾状核 | 7・8 | 1,170 |
| 1 | 冠状断 61 | 被殻 | 9・10 | 1,899 |
| 2 | 冠状断 57 | 淡蒼球外節 | 11・12 | 612 |
| 2 | 冠状断 57 | 淡蒼球内節 | 13・14 | 558 |
| 3 | 水平断 66 | 視床下核 | 5・6 | 256 |
| 4 | 水平断 69 | 黒質 | 3・4 | 574 |
| 5 | 冠状断 49 | 視床 | 15・16 | 2,398 |

すべての対象で画素数は0より大きく、ステッパーの5段階・7対象が監査を通過します。第1段階の冠状断61は、既存の被殻クイズ位置を再利用し、同じ断面で尾状核にも画素があることを確認しています。淡蒼球外節・内節は既存の淡蒼球クイズ位置を再利用しています。

## Papez回路の由来別6段階

Papez回路は、線維路を再構成した表示ではなく、既存教材にある異なる由来の表示を6段階で確認する試作です。断面ラベル、模式3D、CerebrA／Desikan系アトラス3Dを同一種類として扱いません。

| 段階 | 表示区分 | 対象 | 断面・ラベル | 画素数 |
| --- | --- | --- | --- | ---: |
| 1 | 既存クイズ断面ラベル＋既存3D | 海馬 | 冠状断51・ID17/18 | 1,400 |
| 2 | 模式3Dのみ | 脳弓 | 断面ラベルなし | — |
| 3 | 既存クイズ断面ラベルのみ | 乳頭体 | 水平断69・ID39/40 | 120 |
| 4 | 既存クイズ断面ラベル＋既存3D | 視床 | 冠状断49・ID15/16 | 2,398 |
| 5 | アトラス3Dのみ | 帯状回 | 断面ラベルなし | — |
| 6 | アトラス3Dのみ | 海馬傍回・嗅内野 | 断面ラベルなし | — |

乳頭体には原画像由来の3Dメッシュがないため、段階3は「断面ラベルのみ」と表示します。旧模式乳頭体は代用しません。視床は全体の既存ラベルであり、前部核そのものは未分節です。脳弓、帯状回、海馬傍回・嗅内野の段階では断面Canvasを生成しません。乳頭視床路、帯状束、投射線、方向、興奮性／抑制性、新しいmesh・voxelは追加していません。視覚路ステッパーは旧ID33と未分節ID36–38があるため保留します。

再現:

```text
node scripts/audit_pathway_stepper.mjs
node --test tests/pathway-stepper.test.mjs
```

## UIと停止条件

- 「前」「次」「再生」「一時停止」「最初へ戻る」を用意し、最終段階では「次」と「再生」を無効化します。
- 再生は段階インデックスだけを進め、経路線や投射矢印を描きません。
- プリセットを切り替えたとき、自由観察を離れたとき、コンポーネントがアンマウントされたときは `setInterval` をcleanupし、再生状態を停止します。
- スマートフォン幅では断面を3Dの下に縦積みし、操作ボタンの最小高さを44pxとします。
- ステッパーが表示するのは現在の7対象に対応する既存の手動分節ラベルと既存メッシュだけです。ラベルが不明瞭な構造を見た目に合わせて拡張しません。
- Papezは現在段階だけを表示し、手動の自由観察選択へ6段階分を追加しません。別プリセットへ切り替えてもPapezの着色を残しません。

## 2026-08-24 Papez実ブラウザ・機械監査

Windows Chrome 151のローカルproduction preview `http://127.0.0.1:4314` で、6段階の前後移動、最初へ戻る、自動再生、最終段階での自動停止、別回路への切替を実操作しました。海馬・乳頭体・視床はCanvas 2、脳弓・帯状回・海馬傍回／嗅内野はCanvas 1で、乳頭体の「断面ラベルのみ」、視床の「前部核は未分節」、3D専用段階の由来説明を確認しました。loader／UI errorは0件でした。

同じbuildのcanonical route監査は `work/browser-audit/beta-route-audit-papez-stepper-2026-08-24.json` の27経路×3幅×direct/reload＝162/162件に合格しました。cold初回payload監査も `work/performance/initial-route-payload-papez-stepper-2026-08-24.json` の27/27件に合格し、自由観察は13,823,419 encoded bytes・23 requests、既存のexact allowlistだけで14,591,337 bytesの予算内でした。Papez用の新規asset requestはありません。

最終差分では全自動テスト317/317、TypeScript型検査、通常／Pages本番ビルド、beta status監査、Go/No-Go台帳監査、差分検査にも合格しました。Go/No-Go状態は専門家・管理者・公開作業待ちを合格へ繰り上げず、従来どおり未完了として維持しています。

## 未確認事項

最終ローカル本番ビルドをWindows Chrome 151で実操作し、前後移動、5段階の3D・断面同期、再生、最終段階の自動停止、最初へ戻る、Home離脱時の停止と再入場時の初期状態を確認しました。console error/warningは0件でした。1366×768、1024×768、390×768の26経路×direct/reloadも156/156件に合格し、missing/duplicate/fail、console/request/UI error、残留loader、横はみ出し、WebGL fallbackはいずれも0件でした。結果は `work/browser-audit/beta-route-audit-pathway-stepper-final-2026-08-22.json`（ローカル作業用・配布対象外）へ保存しました。全テスト134/134、画素・ID監査、型検査、通常／Pages本番ビルド、差分検査も成功しています。

これらの合格は、専門家による大脳基底核・Papez回路の解剖学的確認、乳頭体の直交断連続性確認、物理端末タッチ操作、公開URL、異なるGPU・別ブラウザでの表示を置き換えません。
