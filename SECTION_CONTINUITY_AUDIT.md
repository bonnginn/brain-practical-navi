# 断面ラベル連続性監査

更新日: 2026-08-15

この監査は、専門家が冠状・水平・矢状の連続断を確認する前に、孤立片や空断面を機械的に洗い出し、目視レビューの優先順位を固定するためのものです。解剖学的な妥当性や境界の正確性を保証するものではありません。

## 再現方法

```powershell
npm run audit:sections
```

`public/atlas/bigbrain-practical-segmentation-icbm500.bin.gz` の35ラベルを左右別のIDのまま読み、次を記録します。

- 冠状・水平・矢状それぞれの空断面数と最大の空白区間
- 6近傍で連結する成分数、最大成分比
- 上位5個の副成分のvoxel数と `x / y / z` 範囲
- validation JSONに記録されたラベル数との一致

回帰閾値は由来別に、手動分節99.95%、アトラス由来95%、画像誘導候補98%の最大成分比としました。これは現データからの意図しない劣化を検出する閾値であり、解剖学的な合格基準ではありません。

## 2026-08-15の結果

- 35/35ラベルでvoxel数がvalidation JSONと一致し、由来別の回帰閾値に合格しました。
- 18ラベルは単一の6近傍成分でした。17ラベルには副成分または投影上の空白があり、目視レビュー対象として警告を残しました。
- 手動分節はすべて最大成分比99.968%以上でした。左尾状核（ID 7）は合計12 voxel、右尾状核（ID 8）は2 voxel、右被殻（ID 10）は3 voxelの微小な副成分を含みます。
- 右扁桃体（ID 22）は最大成分比99.992%ですが、`x168 / y285 / z124` に1 voxelの孤立片があります。この点が投影範囲を延ばすため、矢状・冠状の空白区間としても現れます。
- 左右側脳室（ID 23 / 24）は最大成分比98.532% / 96.388%、第三脳室（ID 25）は95.334%、第四脳室（ID 26）は99.428%でした。細い連結、部分容積、または離れた候補領域を専門家が連続断で確認する必要があります。
- 脳梁候補（ID 30）は2成分で最大成分比98.573%、視交叉（ID 33）は6成分で96.683%でした。
- 左右内包候補（ID 31 / 32）は最大成分比99.966% / 99.967%で、微小な副成分を含みます。

## 専門家レビューの優先順

1. 左右側脳室、第三脳室、第四脳室（ID 23–26）
2. 脳梁候補（ID 30）と視交叉（ID 33）
3. 右扁桃体の1 voxel孤立片（ID 22）
4. 尾状核・被殻・内包候補の微小な副成分（ID 7、8、10、31、32）

手動分節のvoxelは公開由来データとして保存しているため、この監査だけを根拠に削除しません。修正する場合は分節編集フローで差分、対象断面、確認者、採否理由を記録し、専門家レビューを通します。

## Windows実ブラウザ回帰

1366 × 768 pxで冠状・水平・矢状へ順に切り替え、側脳室、第三脳室、第四脳室、脳梁、扁桃体、視交叉の6構造を同時選択して連続断面を自動再生しました。3方向とも断面番号、2方向の切断面3D、断面画像が進行し、方向切替後も選択状態を維持しました。読み込み失敗、画面内例外、横方向のはみ出しは確認されませんでした。

この回帰はUIとデータ表示の動作確認です。左右をまとめる通常学習画面だけではID 22の1 voxelを左右別に採否できないため、専門家は編集画面と座標記録を併用してください。
